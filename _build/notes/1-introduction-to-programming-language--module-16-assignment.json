{
  "module_id": "1-introduction-to-programming-language--module-16-assignment",
  "title": "Assignment: matrices, frequency, pointers and dynamic arrays",
  "overview": "These are Shammunul's answers to the assignment that follows the 2D-array and pointer modules. Each problem reuses one recent tool: a frequency array for counting, a matrix check with a flag, a pointer for a simple calculation, and `realloc` to grow an array one element at a time.",
  "concepts": [
    {
      "term": "Counting while reading",
      "explain": "Update `freq[value]++` as soon as you read each value. Then every later question is a single lookup.",
      "snippet": null
    },
    {
      "term": "\"Appears exactly once\"",
      "explain": "First count every value. Then, on a second pass, count the elements whose frequency is 1.",
      "snippet": null
    },
    {
      "term": "Unit matrix test",
      "explain": "Diagonal cells (`i == j`) must be 1 and every other cell must be 0. Keep the answer in a flag.",
      "snippet": null
    },
    {
      "term": "Absolute difference",
      "explain": "Subtract, then flip the sign if the result is negative. `abs(a - b)` from `<stdlib.h>` does the same.",
      "snippet": null
    },
    {
      "term": "Growing an array with `realloc`",
      "explain": "Start with room for 1 element. Before storing element `i`, resize the block to `i + 1` elements.",
      "snippet": "arr = realloc(arr, (i + 1) * sizeof(int));"
    }
  ],
  "files": [
    {
      "file": "difference.c",
      "title": "Absolute difference using pointers",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Reads `A` and `B` through pointers, subtracts, and makes the result positive.",
      "steps": [
        "Read `A` and `B`, then point `ptr1` at `A` and `ptr2` at `B`.",
        "`diff = *ptr1 - *ptr2`.",
        "If `diff < 0`, set `diff = -diff`.",
        "Print `diff`."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": "6 10\n",
      "input_note": "The sample from the problem.",
      "output_note": "`4`, which matches the expected answer (|6 − 10|).",
      "watch_out": null,
      "check": {
        "q": "What is printed for `10 6`?",
        "a": "4 as well."
      }
    },
    {
      "file": "count_matrix.c",
      "title": "Count occurrences in a matrix",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Builds a frequency array while reading the matrix, then answers each query with `freq[x]`.",
      "steps": [
        "Read `N`, `M` and `X`.",
        "Read the matrix, and add 1 to `freq[Numbers[i][j]]` for each cell.",
        "Read the `X` query numbers into `numbers_to_check[1..X]`.",
        "For each query, print `freq[number]`."
      ],
      "complexity": "Time O(N·M + X) · Space O(max value)",
      "input": "2 3 3\n1 2 5\n2 6 4\n2\n6\n7\n",
      "input_note": "The sample: a 2×3 matrix, then 3 query numbers.",
      "output_note": "`2`, `1` and `0` (2 appears twice, 6 once and 7 never), which matches the expected answer.",
      "watch_out": "The queries are stored starting at index 1. That works because the array has spare room (`X + 5`), but storing from index 0 is less error-prone.",
      "check": {
        "q": "Why is `freq` sized 100005 when values are at most 1000?",
        "a": "It's just a safe upper bound. 1001 slots would be enough."
      }
    },
    {
      "file": "no_repeatation.c",
      "title": "Count numbers that appear only once",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Counts every value, then counts the elements whose frequency is exactly 1.",
      "steps": [
        "Read `N` and the numbers.",
        "First pass: `freq[numbers[i]]++`.",
        "Second pass: add 1 to `count` for each element whose `freq` is 1.",
        "Print `count`."
      ],
      "complexity": "Time O(N) · Space O(max value)",
      "input": "8\n1 4 3 3 5 4 6 3\n",
      "input_note": "8 numbers. 3 and 4 repeat.",
      "output_note": "`3`: the values 1, 5 and 6 appear once each.",
      "watch_out": "The second loop goes over the elements. That is only correct because a value with frequency 1 appears once in the array, so it is counted once.",
      "check": {
        "q": "What is the answer for `2 2 2`?",
        "a": "0."
      }
    },
    {
      "file": "unit_matrix.c",
      "title": "Is it a unit matrix?",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Checks that the diagonal cells are 1 and all other cells are 0, using a flag.",
      "steps": [
        "Read `N` and the N×N matrix.",
        "`flag = 1`.",
        "For each cell: if `i == j`, it must be 1. Otherwise it must be 0. On a mismatch, set `flag = 0` and break out of the inner loop.",
        "Print `YES` if `flag` is still 1, otherwise `NO`."
      ],
      "complexity": "Time O(N²) · Space O(N²)",
      "input": "3\n1 0 0\n0 1 0\n0 2 1\n",
      "input_note": "A 3×3 matrix with one stray 2 off the diagonal.",
      "output_note": "`NO`, because cell (2,1) is 2 instead of 0.",
      "watch_out": null,
      "check": {
        "q": "Is a 1×1 matrix `[1]` a unit matrix?",
        "a": "Yes, so the program prints YES."
      }
    },
    {
      "file": "dynamic_array.c",
      "title": "Grow an array one element at a time",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Starts with a 1-element block and uses `realloc` to make room for each new number before storing it.",
      "steps": [
        "`arr = malloc(sizeof(int))` makes room for 1 element.",
        "For each `i`: read the number into `num_array[i]`.",
        "Call `realloc(arr, (i + 1) * sizeof(int))` so there is room for `i + 1` values, then store `arr[i]`.",
        "Print all `N` values, then `free(arr)`."
      ],
      "complexity": "Time O(N) on average (each `realloc` may copy) · Space O(N)",
      "input": "5\n1 4 2 6 9\n",
      "input_note": "The sample from the problem.",
      "output_note": "`1 4 2 6 9`, which matches the expected answer.",
      "watch_out": "`num_array` is an extra fixed-size array that isn't needed. Read into a plain `int x`. Also check whether `realloc` returned `NULL`.",
      "check": {
        "q": "On the first iteration (i = 0), how big does `realloc` make the block?",
        "a": "1 int, the same size as before."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "Build a frequency array once, then answer many count questions instantly.",
    "\"Appears only once\" means `freq[x] == 1`.",
    "Unit matrix: 1 on the diagonal and 0 everywhere else. Use a flag to check it.",
    "`realloc` lets an array grow as input arrives.",
    "Absolute difference: subtract, then flip the sign if the result is negative."
  ],
  "check_yourself": [
    {
      "q": "How would you find the value that appears most often, using a frequency array?",
      "a": "Count everything, then scan `freq` and keep track of the index with the largest count."
    },
    {
      "q": "Why is it a bad idea to write `arr = realloc(arr, ...)` without checking the result?",
      "a": "If `realloc` fails, it returns NULL. That overwrites your only pointer to the old block, so the memory is lost."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "This is an assignment day, so there is nothing new to learn on this page. Five problems, and each one picks up a tool from the last three modules: a frequency array for counting, a matrix walked with nested loops and a flag, a pointer, and `realloc` for an array that grows as the input arrives.",
        "The work on a day like this is recognising which tool a problem is asking for. Below is a one-paragraph reminder of each, with a tiny example."
      ],
      "snippet": null,
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "**Counting.** The tally sheet from Module 13: the value itself picks the slot. Take the six numbers `1 4 3 3 5 4` and do `freq[x]++` as you read each one.",
        "Now every counting question is one lookup instead of a search. \"How many 3s?\" is `freq[3]`, which is 2. \"How many values appear exactly once?\" is a second walk over the numbers, counting those whose `freq` is 1, and here that is 1 and 5, so the answer is 2. Watch the size: make the tally as big as the largest value the problem allows, not as big as `N`.",
        "This does not change when the numbers arrive in a grid instead of a line. A matrix cell is just another number, so tick `freq[a[i][j]]++` while you read the matrix, and answer the queries afterwards."
      ],
      "snippet": null,
      "trace": "value   1   2   3   4   5\ncount   1   0   2   2   1"
    },
    {
      "heading": null,
      "body": [
        "**Checking a matrix.** Two loops over every cell, and one flag holding the answer. Start at `flag = 1`, meaning \"true so far\", and clear it on the first cell that breaks the rule. For a unit matrix the rule is: on the diagonal (`i == j`) the cell must be 1, everywhere else it must be 0.",
        "Watch out for two things here. A `break` inside the inner loop only leaves the inner loop, so the outer one keeps running; the answer is still right, but nothing is saved. And the rows and columns count from 0, so cell (2,1) is the second column of the third row."
      ],
      "snippet": null,
      "trace": "1 0 0    i==j -> must be 1  ok\n0 1 0    others -> must be 0  ok\n0 2 1    (2,1) is 2, not 0   -> NO"
    },
    {
      "heading": null,
      "body": [
        "**Pointers.** A pointer holds an address, and the star follows it. After `int *ptr1 = &A;` the expression `*ptr1` is `A` itself, so `*ptr1 - *ptr2` is the same subtraction you would write with plain `A - B`. For an absolute difference, subtract and then flip the sign if the result came out negative.",
        "**Growing an array.** Ask for one element with `malloc(sizeof(int))`, then before storing element `i`, resize the block so it has room for `i + 1` values. `realloc` keeps everything already in the block, so the earlier numbers survive each step:"
      ],
      "snippet": "int *arr = malloc(sizeof(int));\nfor(int i = 0; i < n; i++){\n    int x;\n    scanf(\"%d\", &x);\n    arr = realloc(arr, (i + 1) * sizeof(int));\n    arr[i] = x;\n}\nfree(arr);",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Assign the result of `realloc` back to the pointer, because the block may have been moved to a new address. On a judge, none of these five problems is hard once it has been matched to its tool; what costs marks is the small print. Size the frequency array from the stated limits, count matrix indexes from 0, and print exactly what is asked, such as `YES` or `NO` in capitals and one answer per line.",
        "The programs below are Shammunul's answers, each shown with the input it was run on and with the awkward parts noted."
      ],
      "snippet": null,
      "trace": null
    }
  ]
}