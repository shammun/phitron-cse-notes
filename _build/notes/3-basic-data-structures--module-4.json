{
  "module_id": "3-basic-data-structures--module-4",
  "title": "Assignment: vectors, prefix sums & patterns",
  "overview": "This module is an assignment that checks the tools from modules 1 to 3. You find duplicates by sorting, print a prefix-sum array backwards, check if arrays are sorted, insert one vector into another, and draw an X pattern. Each solution is a short, typical judge program.",
  "intro": [
    {
      "heading": null,
      "body": [
        "An assignment day does not teach a new tool. It checks one thing: can you look at a problem and say which of the tools from the last three modules it is asking for. That recognition is the whole skill, so it is worth naming the shapes before you read the problems.",
        "Three shapes cover almost everything on this page. A question about **totals over a stretch of the array** wants a prefix sum. A question about **repeats, or about order**, wants a sort first. A question about **drawing a picture** wants nested loops and careful counting of spaces."
      ],
      "snippet": null,
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Start with the prefix sum, because it is the one people half-remember. You are given `2 4 1 5 3`. The prefix sum array holds, at each position, the total of everything up to and including that position. You build it in one pass, each entry being the previous entry plus the current value:"
      ],
      "snippet": null,
      "trace": "i        0    1    2    3    4\na[i]     2    4    1    5    3\np[i]     2    6    7   12   15\n              p[i] = p[i-1] + a[i]"
    },
    {
      "heading": null,
      "body": [
        "Once `p` exists, the sum of any stretch is one subtraction, and printing the array backwards is only a loop running from the end. The trap is the size of the numbers. A hundred thousand values of up to 1000000000 each add up to about 10^14, while an `int` stops near 2147483647. The sums have to live in `long long`, and the overflow does not announce itself — you simply get a wrong number.",
        "The second shape. Are there two equal values anywhere among 100000 numbers? Comparing every pair is about 5000000000 comparisons, far too slow. Sort first and equal values land side by side, so one walk comparing each element with its left neighbour is enough:"
      ],
      "snippet": null,
      "trace": "given    2  1  3  5  2  1\nsorted   1  1  2  2  3  5\n         ^--^  neighbours equal -> duplicate"
    },
    {
      "heading": null,
      "body": [
        "The same walk with `<` instead of `==` answers a different question: is the array already sorted? Keep a flag, set it to false the moment one element is smaller than the one before it, and stop there.",
        "The third shape is moving things about. Inserting the whole of `10 20 30` into `2 3 4 5 6` at index 3 gives `2 3 4 10 20 30 5 6`. With a vector you can do it one value at a time with `insert`, but the position must move forward after each one, or the inserted piece comes out reversed:"
      ],
      "snippet": null,
      "trace": "A = 2 3 4 5 6\ninsert 10 at 3 -> 2 3 4 10 5 6\ninsert 20 at 4 -> 2 3 4 10 20 5 6\ninsert 30 at 5 -> 2 3 4 10 20 30 5 6"
    },
    {
      "heading": null,
      "body": [
        "And the drawing shape. Do not try to see the whole picture at once; count one row at a time. For an X of size 5 the top row is `\\   /` — no leading spaces, three spaces in the middle. Every row below adds one leading space and takes two away from the middle, until the middle is empty and the two strokes meet at the `X`. Then the bottom half does the reverse.",
        "Two last things that quietly cost marks. When the input begins with T test cases, everything you set up — the vector, the flag — must be created fresh inside the loop, not once outside it. And match the output format exactly, capitals and spacing included.",
        "The five programs below are these shapes, one each, written the way you would write them with a judge waiting."
      ],
      "snippet": null,
      "trace": null
    }
  ],
  "concepts": [
    {
      "term": "Sort to find duplicates",
      "explain": "After sorting, equal values sit next to each other, so one pass comparing `A[i]` with `A[i-1]` finds any duplicate.",
      "snippet": "sort(A.begin(), A.end());"
    },
    {
      "term": "Prefix sums need long long",
      "explain": "Values up to 10^9 add up past the `int` limit quickly, so the running sum must be `long long`.",
      "snippet": "vector<long long> pre(n);"
    },
    {
      "term": "Sorted check",
      "explain": "An array is sorted ascending if no element is smaller than the one before it. One broken pair is enough to answer NO.",
      "snippet": "if(A[j] < A[j-1]) flag = false;"
    },
    {
      "term": "vector::insert at a position",
      "explain": "`A.insert(A.begin() + X, value)` puts a value at index `X` and shifts the rest right. Increase `X` after each insert to keep the inserted block in order.",
      "snippet": null
    },
    {
      "term": "Pattern printing by counting spaces",
      "explain": "For each row, work out how many spaces come before, between and after the characters, and how those counts change from row to row.",
      "snippet": null
    },
    {
      "term": "Multiple test cases",
      "explain": "Read `T` first, then repeat the whole read-solve-print routine `T` times, resetting variables inside the loop.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "assignment_1_duplicate.cpp",
      "title": "Duplicate",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Prints YES if any value appears twice. It sorts the vector and compares neighbours.",
      "steps": [
        "Read `n` and the vector `A`.",
        "`sort` it so equal values become neighbours.",
        "For `i` from 1, if `A[i] == A[i-1]`, print `YES` and exit.",
        "If the loop ends, print `NO`."
      ],
      "complexity": "Time O(n log n) · Space O(n)",
      "input": "6\n2 1 3 5 2 1\n",
      "input_note": "n, then n numbers.",
      "output_note": "`YES`: after sorting (`1 1 2 2 3 5`) the first two values are equal.",
      "watch_out": null,
      "check": {
        "q": "Why is a frequency array a bad idea here?",
        "a": "Values go up to 10^9, far too large for an array index."
      }
    },
    {
      "file": "assignment_2_get_prefix_sum.cpp",
      "title": "Get prefix sum (reversed)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Builds the prefix-sum array with `long long` and prints it from the last position to the first.",
      "steps": [
        "Read the values into `vector<long long> nums`.",
        "`runningSum[0] = nums[0]`, then `runningSum[i] = runningSum[i-1] + nums[i]`.",
        "Print `runningSum` from index `n-1` down to 0."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "3\n1000000000 1000000000 1000000000\n",
      "input_note": "n, then n numbers (this case tests overflow).",
      "output_note": "`3000000000 2000000000 1000000000`: the totals are larger than an `int` can hold, and they print correctly because of `long long`.",
      "watch_out": null,
      "check": {
        "q": "What would this sample print if the sums were `int`?",
        "a": "Wrong, overflowed (negative) numbers for 3000000000 and 2000000000."
      }
    },
    {
      "file": "assignment_3_sorted.cpp",
      "title": "Sorted",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "For each test case, prints YES if the array is in non-decreasing order, else NO.",
      "steps": [
        "Read `T`.",
        "For each test, read `N` and `A`.",
        "Start `flag = true`; if any `A[j] < A[j-1]`, set it false and `break`.",
        "Print `YES` or `NO`."
      ],
      "complexity": "Time O(N) per test · Space O(N)",
      "input": "3\n5\n2 4 6 7 10\n4\n1 1 2 2\n4\n100 1 102 12\n",
      "input_note": "T, then for each test: N and the array.",
      "output_note": "`YES` (strictly rising), `YES` (equal neighbours are allowed), `NO` (1 comes after 100).",
      "watch_out": null,
      "check": {
        "q": "Why use `<` and not `<=` in the comparison?",
        "a": "Equal neighbours still count as sorted ascending, so only a strict drop breaks the order."
      }
    },
    {
      "file": "asasignment_4_insert_it.cpp",
      "title": "Insert it",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Inserts every element of B into A starting at index X, keeping B's order, then prints A.",
      "steps": [
        "Read `A` (size `N`), `B` (size `M`) and `X`.",
        "For each `B[i]`, call `A.insert(A.begin() + X, B[i])`.",
        "Then `X++`, so the next element goes right after the one just inserted.",
        "Print all `N + M` elements."
      ],
      "complexity": "Time O(M * (N + M)) · Space O(N + M)",
      "input": "5\n2 3 4 5 6\n3\n10 20 30\n3\n",
      "input_note": "N, A, M, B, then X.",
      "output_note": "`2 3 4 10 20 30 5 6`: B sits in the middle, starting at index 3.",
      "watch_out": "Without `X++`, B would end up reversed (`30 20 10`). One call `A.insert(A.begin() + X, B.begin(), B.end());` does the whole job in O(N + M).",
      "check": {
        "q": "What does the program print for X = 0 with the same arrays?",
        "a": "`10 20 30 2 3 4 5 6`."
      }
    },
    {
      "file": "assignment_5_printing_X.cpp",
      "title": "Printing X",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Draws an X of height `N` (odd) with `\\` and `/`, putting `X` in the middle row.",
      "steps": [
        "Special case: `N == 1` prints just `X`.",
        "Top half (`N/2` rows): row `i` has `i` spaces, `\\`, `spaces` gaps, `/`, and `i` spaces. `spaces` starts at `N-2` and drops by 2 each row.",
        "Middle row: `N/2` spaces then `X`.",
        "Bottom half: `space_before_after` starts at `N/2 - 1` and shrinks by 1; `space_between` starts at 1 and grows by 2; each row prints `/`, the gap, `\\`."
      ],
      "complexity": "Time O(N^2) · Space O(1)",
      "input": "7\n",
      "input_note": "A single odd N.",
      "output_note": "A 7-line X: the arms meet at the `X` in row 4. In the top half the gap between `\\` and `/` shrinks 5, 3, 1; in the bottom half it grows 1, 3, 5.",
      "watch_out": "The middle row has no trailing spaces while the other rows do. Most judges ignore trailing spaces, but a strict checker would not. Remember `\\` in a C++ string prints one backslash.",
      "check": {
        "q": "How many spaces are between the slashes on the first row when N = 9?",
        "a": "7 (N - 2)."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "Sort first when you need to spot equal values.",
    "Use `long long` for any running total of large numbers.",
    "An array is sorted if no neighbour pair goes down.",
    "Increase the insert position after each `insert` to keep the inserted block's order.",
    "Pattern problems: write down the space counts per row, then find how they change."
  ],
  "check_yourself": [
    {
      "q": "How can you check for duplicates in O(n) average time without sorting?",
      "a": "Insert values into an `unordered_set` and report YES when an insert finds the value already present."
    },
    {
      "q": "What is the time complexity of inserting M elements one by one at the front of a vector of size N?",
      "a": "O(M * (N + M)), because each insert shifts every element after it."
    }
  ],
  "resources": [
    {
      "kind": "article",
      "name": "GeeksforGeeks — prefix sums",
      "url": "https://www.geeksforgeeks.org/prefix-sum-array-implementation-applications-competitive-programming/",
      "note": "The trick and the problems it solves."
    },
    {
      "kind": "visual",
      "name": "USFCA — linear vs binary search",
      "url": "https://www.cs.usfca.edu/~galles/visualization/Search.html",
      "note": "Run both on the same array and count the steps."
    },
    {
      "kind": "article",
      "name": "Striver's A2Z DSA sheet",
      "url": "https://takeuforward.org/strivers-a2z-dsa-course/strivers-a2z-dsa-course-sheet-2/",
      "note": "An ordered list of problems per topic, easy ones first."
    }
  ]
}