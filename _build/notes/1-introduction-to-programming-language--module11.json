{
  "module_id": "1-introduction-to-programming-language--module11",
  "title": "Strings: length and <string.h> functions",
  "overview": "Module 11 goes deeper into C strings. A string is a char array ending in the null character `'\\0'`, and most string work is built on that one fact. You count a string's length by hand, then with `strlen`, and you meet the other `<string.h>` helpers: `strcpy`, `strcat` and `strcmp`.",
  "concepts": [
    {"term": "Null terminator `'\\0'`", "explain": "Every C string ends with a hidden `'\\0'` character. Functions stop reading when they reach it, not when the array ends.", "snippet": "char s[10] = \"abcd\"; // stores a b c d \\0 and 5 unused slots"},
    {"term": "Counting length by hand", "explain": "Loop until you hit `'\\0'` and count the steps. This is what `strlen` does for you.", "snippet": "int len = 0;\nwhile(s[len] != '\\0') len++;"},
    {"term": "`strlen`", "explain": "Found in `<string.h>`. It returns how many characters come before the first `'\\0'`. Spaces and punctuation count too.", "snippet": null},
    {"term": "`strcpy` and `strcat`", "explain": "`strcpy(dst, src)` overwrites `dst` with `src`. `strcat(dst, src)` adds `src` to the end of `dst`. Either way, `dst` must be big enough to hold the result.", "snippet": "strcpy(b, a); // b now holds the same text as a"},
    {"term": "`strcmp` and dictionary order", "explain": "`strcmp(x, y)` compares the strings letter by letter. It returns a negative number if `x` comes first in dictionary (lexicographic) order, 0 if the strings are equal, and a positive number if `x` comes later.", "snippet": "strcmp(\"apple\", \"apply\") < 0  // 'e' < 'y'"}
  ],
  "files": [
    {
      "file": "string_length.c",
      "title": "String length with a loop",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Counts the characters in `\"abcd\"` without any library function. The loop walks the array until it reaches the null terminator.",
      "steps": [
        "`char str[10] = \"abcd\";` sets aside 10 slots but uses only 5: four letters and `'\\0'`.",
        "`length` starts at 0.",
        "The `for` loop runs while `str[i] != '\\0'`, so it visits indexes 0, 1, 2 and 3.",
        "Each pass adds 1 to `length`. At index 4 the loop finds `'\\0'` and stops.",
        "`printf(\"%d\", length)` prints the count."
      ],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "It prints `4`: the number of letters, not the array size 10. There is no newline after it.",
      "watch_out": "Don't loop up to the array size (`i < 10`). The slots after `'\\0'` are not part of the string.",
      "check": {"q": "What would this program print if the string were `\"hi there\"`?", "a": "8, because the space counts as a character."}
    },
    {
      "file": "string_length_function.c",
      "title": "String length with strlen",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Gets the same kind of answer with `strlen` from `<string.h>`. The string here has a space and a `?` in it, to show that every character counts.",
      "steps": [
        "`#include <string.h>` makes `strlen` available.",
        "`char str[10] = \"abcd ab?\";` holds 8 characters plus `'\\0'`, so 9 of the 10 slots are used.",
        "`strlen(str)` counts characters until the first `'\\0'`.",
        "The result goes into `length` and is printed."
      ],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "It prints `8`: a, b, c, d, the space, a, b and `?`.",
      "watch_out": "If a string has `'\\0'` in the middle (for example `\"hi\\0there\"`), `strlen` stops at the first one and returns 2.",
      "check": {"q": "What is the longest string that fits in `char str[10]`?", "a": "9 characters, because one slot must hold `'\\0'`."}
    }
  ],
  "practice_links": [],
  "recap": [
    "A C string is a char array that ends with `'\\0'`.",
    "The length is the number of characters before `'\\0'`, including spaces.",
    "`strlen`, `strcpy`, `strcat` and `strcmp` all live in `<string.h>`.",
    "`strcmp` returns a negative number, zero or a positive number depending on dictionary order.",
    "Always leave one extra slot for the terminator."
  ],
  "check_yourself": [
    {"q": "Which comes first in dictionary order, `apples` or `apply`, and why?", "a": "`apples`. The first four letters match, and then `e` comes before `y`."},
    {"q": "After `strcpy(b, a)`, what happened to the text that was in `b` before?", "a": "It is overwritten. `b` now holds a copy of `a`."},
    {"q": "What does `strlen(\"hello world\")` return?", "a": "11, because the space counts."}
  ]
}
