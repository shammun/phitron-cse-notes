{
  "module_id": "1-introduction-to-programming-language--module-20-final-exam",
  "title": "Final exam: matrices, functions, big numbers and strings",
  "overview": "These are Shammunul's final-exam answers for the Introduction to Programming course. They combine the whole course: matrix checks on both diagonals, swapping rows and columns through a pointer-based `swap`, `long long` arithmetic with divisibility, and comparing strings position by position.",
  "concepts": [
    {"term": "Both diagonals at once", "explain": "A cell is on a diagonal if `i == j || i + j == n - 1`. Everything else is off-diagonal.", "snippet": null},
    {"term": "Checker function that returns early", "explain": "A function that returns 0 at the first broken rule and 1 at the end is cleaner than a flag.", "snippet": null},
    {"term": "Reusable `swap(int*, int*)`", "explain": "Pass the addresses of any two cells, such as `&m[i][0]` and `&m[i][M-1]`, to exchange them.", "snippet": null},
    {"term": "Divisibility test", "explain": "`M % P == 0` means `P` divides `M` exactly, and then the missing factor is `M / P`.", "snippet": null},
    {"term": "`long long` and `%lld`", "explain": "Values up to 10^18 need 64-bit integers, and scanf/printf must use `%lld`.", "snippet": null},
    {"term": "Per-position counting", "explain": "For three strings of equal length, each position can be fixed on its own. If all three letters differ, it costs 2 changes. If exactly one differs, it costs 1. If all match, it costs 0.", "snippet": null}
  ],
  "files": [
    {
      "file": "jadu_matrix.c",
      "title": "Jadu matrix (both diagonals are 1)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "`jaduMatrix` returns 1 if the matrix is square, both diagonals are all 1, and every other cell is 0.",
      "steps": [
        "Read `N`, `M` and the matrix into a fixed `105×105` array.",
        "If `N != M`, return 0, because only a square matrix can qualify.",
        "For each cell on either diagonal (`i == j || i + j == N - 1`), the value must be 1.",
        "Every other cell must be 0. Any failure returns 0 at once.",
        "If the loops finish, return 1, and `main` prints `YES` or `NO`."
      ],
      "complexity": "Time O(N²) · Space O(1) extra",
      "input": "3 3\n1 0 1\n0 1 0\n1 0 1\n",
      "input_note": "A 3×3 matrix shaped like an X.",
      "output_note": "`YES`. The centre cell (1,1) is on both diagonals and is 1.",
      "watch_out": "The parameter `int matrix[105][105]` must match how the array is declared. C needs the column size to work out where each row starts.",
      "check": {"q": "In a 6×6 matrix, which cells of row 2 are on a diagonal?", "a": "Columns 2 and 3, since i == j gives j = 2 and i + j = 5 gives j = 3."}
    },
    {
      "file": "swap_matrix.c",
      "title": "Swap first/last column, then first/last row",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Uses a pointer `swap` to exchange the first and last columns, then the first and last rows, and prints the result.",
      "steps": [
        "Read `N`, `M` and the matrix.",
        "For each row `i`: `swap(&matrix[i][0], &matrix[i][M-1])`.",
        "For each column `j`: `swap(&matrix[0][j], &matrix[N-1][j])`.",
        "Print the grid, one row per line."
      ],
      "complexity": "Time O(N·M) · Space O(N·M)",
      "input": "3 4\n1 2 3 4\n5 6 7 8\n6 5 4 2\n",
      "input_note": "The sample from the problem.",
      "output_note": "`2 5 4 6` / `8 6 7 5` / `4 2 3 1`, which matches the expected answer.",
      "watch_out": "The order matters. The problem swaps columns first and rows second, so the corners end up diagonally opposite their starting places.",
      "check": {"q": "Where does the top-left value `1` end up?", "a": "In the bottom-right corner: the column swap moves it to the right, and the row swap moves it to the bottom."}
    },
    {
      "file": "missing_number.c",
      "title": "Find the missing factor",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "The four numbers multiply to `M`, and three of them are known. The fourth is `M / (A·B·C)` if that divides exactly. Otherwise the answer is -1.",
      "steps": [
        "Read `T`, the number of test cases.",
        "For each case, read `M`, `A`, `B` and `C` as `long long`.",
        "If `M % (A*B*C) == 0`, store `M / (A*B*C)`. Otherwise store -1.",
        "After the loop, print all the stored results, one per line."
      ],
      "complexity": "Time O(T) · Space O(T)",
      "input": "3\n20 5 1 1\n18 2 3 4\n0 10 20 30\n",
      "input_note": "3 cases: one that divides, one that doesn't, and M = 0.",
      "output_note": "`4` (20/5), then `-1` (18 isn't divisible by 24), then `0` (0 divided by anything is 0, so the missing number is 0).",
      "watch_out": "`A*B*C` can reach 10^18, which still fits in a `long long`. Storing results isn't needed: you could print each answer straight away. `long long results[100005]` uses about 800 KB of stack.",
      "check": {"q": "Why is there no risk of dividing by zero?", "a": "The limits say A, B and C are at least 1, so their product is never 0."}
    },
    {
      "file": "same_same_but_different.c",
      "title": "Minimum changes to make 3 strings equal",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Looks at each position on its own. If all three letters differ, 2 changes are needed. If exactly two match, 1 change is needed. If all three match, none is needed.",
      "steps": [
        "Read the three strings and take `len` from the first.",
        "For each `i`: if all three characters differ, add 2.",
        "Otherwise, if exactly one character is the odd one out (three separate `else if` cases), add 1.",
        "Otherwise all three match, so add 0.",
        "Print `total_changes`."
      ],
      "complexity": "Time O(len) · Space O(len)",
      "input": "train\ncandy\nbread\n",
      "input_note": "The sample from the problem: three words of 5 letters.",
      "output_note": "`9`, which matches the expected answer. Four positions have three different letters (2 each), and position 1 has `r a r` (1 change).",
      "watch_out": "The three `else if` cases could be merged: once \"all different\" is ruled out, the cost is 0 if all three match and 1 otherwise.",
      "check": {"q": "What is the cost for position letters `x x x`, `x y x` and `x y z`?", "a": "0, 1 and 2."}
    },
    {
      "file": "max_number.c",
      "title": "Mountain rows 1..N..1 (header says Max Number)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "This is the same code as `max_number.c` in Module 19, with comments added. The header describes the Max Number problem, but the code prints `1 … N … 1` for each test case.",
      "steps": [
        "Read `T` and then the `T` values.",
        "`firstPart(n)` prints 1 up to n.",
        "`secondPart(n)` prints n−1 down to 1.",
        "Print a newline after each row."
      ],
      "complexity": "Time O(sum of N) · Space O(T)",
      "input": "2\n3\n4\n",
      "input_note": "T = 2, then N = 3 and N = 4.",
      "output_note": "`1 2 3 2 1` and `1 2 3 4 3 2 1`, each with a trailing space.",
      "watch_out": "**The header and the code don't match.** The pattern code is fine for its own task, but it doesn't find the maximum of an array. Rewrite the header, or solve Max Number with recursion (see the Module 19 notes).",
      "check": {"q": "How many numbers are in the row for N = 4?", "a": "7, which is 2N − 1."}
    }
  ],
  "practice_links": [],
  "recap": [
    "Diagonal test: `i == j || i + j == n - 1`, and this only applies to square matrices.",
    "A checker function that returns early is cleaner than a flag.",
    "One pointer `swap` works for any two cells.",
    "Use `%` to check for exact division, and use `long long` with `%lld` for values up to 10^18.",
    "Break string problems into independent positions when you can."
  ],
  "check_yourself": [
    {"q": "Why must `jaduMatrix` reject `N != M` first?", "a": "Diagonals are only defined for square matrices, and `i + j == N - 1` makes no sense otherwise."},
    {"q": "For three letters at one position, why is the cost never more than 2?", "a": "You can change two of them to match the third."},
    {"q": "What does `printf(\"%d\", x)` do when `x` is a `long long`?", "a": "The behaviour is undefined, and it often prints a wrong value. Use `%lld`."}
  ]
}
