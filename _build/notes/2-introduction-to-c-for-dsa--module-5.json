{
  "module_id": "2-introduction-to-c-for-dsa--module-5",
  "title": "The C++ string class",
  "overview": "Module 5 replaces char arrays with `std::string`. You assign and compare with ordinary operators, read characters safely, change strings with built-in modifiers, walk them with iterators, read whole lines with `getline`, and split lines into words with `stringstream`.",
  "concepts": [
    {
      "term": "string vs char array",
      "explain": "`string` grows by itself and supports `=`, `==`, `+`; char arrays need `strcpy`/`strcmp`.",
      "snippet": null
    },
    {
      "term": "Element access",
      "explain": "`s[i]`, `s.at(i)`, `s.front()`, `s.back()`.",
      "snippet": null
    },
    {
      "term": "Capacity",
      "explain": "`size()`, `capacity()`, `clear()`, `empty()`, `resize()`.",
      "snippet": null
    },
    {
      "term": "Modifiers",
      "explain": "`+=`, `append`, `push_back`, `pop_back`, `insert`, `erase`, `replace`, `assign`.",
      "snippet": "s.replace(6, 5, \"Bangladesh\");"
    },
    {
      "term": "Iterators",
      "explain": "`begin()` is the first element, `end()` is one past the last.",
      "snippet": "for (auto it = s.begin(); it != s.end(); it++) cout << *it;"
    },
    {
      "term": "getline",
      "explain": "`getline(cin, s)` reads a whole line into a string.",
      "snippet": null
    },
    {
      "term": "stringstream",
      "explain": "Turns a string into an input stream so `>>` can split it into words.",
      "snippet": "stringstream ss(line); while (ss >> w) count++;"
    }
  ],
  "files": [
    {
      "file": "string.cpp",
      "title": "C strings vs the string class",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Shows that `string` can be assigned with `=` and compared with `==`, while C char arrays need `strcpy` and `strcmp`.",
      "steps": [
        "`string s = \"hello\"; s = \"world\";` just works.",
        "`r == r2` compares the text of two strings.",
        "A string can be given a longer value at any time - it grows by itself."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "`world`, `Equal`, then `Hello, how are you?`.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "What C function does `r == r2` replace?",
        "a": "`strcmp(r, r2) == 0`."
      }
    },
    {
      "file": "element_access.cpp",
      "title": "Accessing characters: [], at, front, back",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Reads one word and prints its first and last characters in different ways.",
      "steps": [
        "`s[0]` and `s.at(0)` both give the first character (`at` also checks the index).",
        "`s.back()` and `s[s.size()-1]` give the last character.",
        "`s.front()` is another way to get the first."
      ],
      "complexity": "Time O(1)",
      "input": "Phitron\n",
      "input_note": "One word.",
      "output_note": "For `Phitron`: the first character is `P` (three ways) and the last is `n` (two ways).",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "What is the difference between `s[10]` and `s.at(10)` on a short string?",
        "a": "`s[10]` is undefined behaviour; `s.at(10)` throws an `out_of_range` exception."
      }
    },
    {
      "file": "string_capacity_functions.cpp",
      "title": "size, capacity, clear, empty, resize",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Explores the capacity functions of `string`.",
      "steps": [
        "`s.size()` is the number of characters (5 for Hello).",
        "`s.max_size()` is the theoretical limit; `s.capacity()` is the room already reserved.",
        "`s.clear()` empties the text but keeps the capacity.",
        "`s.empty()` checks for length 0.",
        "`s.resize(n)` cuts or pads; `resize(n, 'x')` pads with `x`."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "Size `5`, a huge max size, capacity `15` (GCC keeps 15 characters inside the object). After `clear()` size is 0 but capacity is still 15. `resize(10)` gives `I would li`; `resize(15)` pads with invisible null characters; `resize(15, 'x')` gives `I would lixxxxx`.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "After `s.clear()`, what are `s.size()` and `s.empty()`?",
        "a": "`0` and `true`."
      }
    },
    {
      "file": "string_modifiers.cpp",
      "title": "Modifying strings",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Tries `+=`, `append`, `insert`, `push_back`, `pop_back`, `assign`, `erase` and `replace` one after another.",
      "steps": [
        "`s += s2` and `s.append(...)` add text at the end.",
        "`s.insert(5, \" C++ \")` puts text at index 5.",
        "`push_back('!')` adds one char; `pop_back()` removes the last.",
        "`assign` works like `=`.",
        "`erase(5)` keeps only the first 5 chars; `erase(1, 2)` removes 2 chars from index 1.",
        "`replace(6, 5, X)` swaps 5 chars at index 6 for X; with length 0 it just inserts."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "Follow `s`: `Hello world, Hi there` -> insert gives `Hello C++  world, Hi there` -> `s[0]='K'` makes it `Kello...` -> `erase(5)` gives `Kello` -> `erase(1, 2)` gives `Klo`. `s4` becomes `Hello Bangladesh`, then `Hello BangladeshBangladesh`.",
      "watch_out": "The line after `s[0] = 'K'` prints `s2` (still `Rahim`) although the label talks about the change; the change is to `s`, which you only see in the pop_back line.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "What does `\"Kello\".erase(1, 2)` leave?",
        "a": "`Klo`"
      }
    },
    {
      "file": "iterators.cpp",
      "title": "Iterators: begin() and end()",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "`begin()` points at the first character and `end()` points one past the last. Loops can walk a string with an iterator or with `auto`.",
      "steps": [
        "Print each character with an index loop.",
        "`*s.begin()` is `H`; `*(s.end()-1)` is the last character `!`.",
        "`string::iterator it` walks from `begin()` to `end()`, printing `*it`.",
        "The same loop with `auto it` is shorter."
      ],
      "complexity": "Time O(n)",
      "input": null,
      "input_note": null,
      "output_note": "Line 2 ends with `H` because the index loop has no newline before `*s.begin()`. The next line looks blank: it is `*s.end()`. Then `!`, and two columns of letters from the two iterator loops.",
      "watch_out": "`*s.end()` is undefined behaviour; it happens to print the hidden null character here. Only dereference `end() - 1`.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Which character does `s.end()` point to?",
        "a": "None - it points one past the last character, so it must not be dereferenced."
      }
    },
    {
      "file": "string_input_with_space.cpp",
      "title": "getline for strings",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "`getline(cin, s)` reads a whole line into a `string`. After reading a number, `cin.ignore()` is needed before the next `getline`.",
      "steps": [
        "`getline(cin, s)` reads the first line.",
        "`cin >> x` reads a number.",
        "`cin.ignore()` removes the newline.",
        "`getline(cin, s2)` reads the next full line."
      ],
      "complexity": "Time O(n)",
      "input": "Learning C++ strings\n25\nBangladesh is beautiful\n",
      "input_note": "A line, a number, then another line.",
      "output_note": "The first line, `25`, then `Bangladesh is beautiful` - both lines kept their spaces.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "How is `getline(cin, s)` different from `cin.getline(s, 100)`?",
        "a": "The first reads into a `string` (no size limit); the second reads into a char array."
      }
    },
    {
      "file": "stringstream.cpp",
      "title": "Splitting words with stringstream",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Wraps a line in a `stringstream` so `>>` can pull out one word at a time; used to split lines and count words.",
      "steps": [
        "`stringstream ss(s)` makes a stream from the line.",
        "Each `ss >> word` takes the next word.",
        "`while (ss2 >> word)` prints every word of line 2.",
        "The third loop also counts the words."
      ],
      "complexity": "Time O(n)",
      "input": "I love to code daily\nbig brown fox\n  one two   three four  \n",
      "input_note": "Three lines of words (the third has extra spaces).",
      "output_note": "First four words `I`, `love`, `to`, `code` (the fourth label wrongly says \"third\"). Then `big`, `brown`, `fox`. The third line gives four words and the count `4` - extra spaces are ignored.",
      "watch_out": "The fourth print is labelled \"third word\" by mistake.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why does the count ignore the extra spaces?",
        "a": "`>>` skips any whitespace before each word."
      }
    }
  ],
  "practice_links": [
    {
      "name": "A. Way Too Long Words",
      "url": "https://codeforces.com/problemset/problem/71/A",
      "site": "Codeforces"
    },
    {
      "name": "A. Anton and Danik",
      "url": "https://codeforces.com/problemset/problem/734/A",
      "site": "Codeforces"
    }
  ],
  "recap": [
    "Prefer `string` to char arrays.",
    "`at()` checks bounds, `[]` does not.",
    "`end()` is one past the last character - never dereference it.",
    "`cin.ignore()` before `getline` after reading a number.",
    "`stringstream` + `while (ss >> w)` splits a line into words."
  ],
  "check_yourself": [
    {
      "q": "What does `s.erase(3)` do?",
      "a": "Removes everything from index 3 to the end."
    },
    {
      "q": "How do you count words in a line that may have several spaces between words?",
      "a": "Put the line in a `stringstream` and count how many times `ss >> word` succeeds."
    },
    {
      "q": "What is the difference between `size()` and `capacity()`?",
      "a": "`size()` is the number of characters stored; `capacity()` is how many fit before the string must grow."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "You already know how to hold text in C: a `char` array with a fixed size and a hidden `'\\0'` at the end. It works, but every small job needs care. You must guess the size when you write the program, you cannot copy with `=`, you cannot compare with `==`, and joining two pieces means `strcat` and hoping the first array is big enough.",
        "Take a plain job: read a name, put `Hello, ` in front of it, and check whether the name is `Rahim`. In C you would write something like this, and you would have to know that `name[100]` is long enough for anything the user types."
      ],
      "snippet": "char name[100], msg[200];\ncin >> name;\nstrcpy(msg, \"Hello, \");\nstrcat(msg, name);\nif(strcmp(name, \"Rahim\") == 0) cout << \"known\";",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Module 5 replaces all of that with one type, `string`, and the same four lines become the lines you would have written first if nobody had told you the rules.",
        "Here is the same list of jobs in both forms. Nothing in the left column stops working; the right column is just shorter and safer."
      ],
      "snippet": null,
      "trace": "job              C char array        C++ string\n---------------------------------------------------\ndeclare          char s[100];        string s;\nread a word      cin >> s;           cin >> s;\ncopy into it     strcpy(s, \"hi\");    s = \"hi\";\njoin two         strcat(a, b);       a = a + b;\nsame text?       strcmp(a,b) == 0    a == b\nhow long?        strlen(s)           s.size()\none character    s[i]                s[i]"
    },
    {
      "heading": null,
      "body": [
        "**What is genuinely new is the size.** With `char s[100]` the size was decided when you typed the program, and 101 characters would run off the end. A `string` decides its size while the program runs and grows whenever you give it more text. It also keeps its own length, so `s.size()` is a number it already knows, not a loop over the characters looking for `'\\0'` the way `strlen` works.",
        "So this is allowed, and each line simply makes the string longer:"
      ],
      "snippet": "string s = \"Dhaka\";\ns = \"Dhaka, Bangladesh\";   // longer - fine\ns += \" is my home\";        // longer again\ncout << s.size() << endl;  // 28",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "**A second example: reading.** `cin >> s` still stops at the first space, so `Dhaka city` would give you only `Dhaka`. For a whole line you used `cin.getline(buf, 100)` with a char array; with a string it is `getline(cin, s)` and there is no size to pass. The old trap stays: after `cin >> n` a newline is still waiting in the input, so `cin.ignore()` goes between the number and the next `getline`.",
        "**A third example: splitting.** Once a line is in a string, `stringstream ss(s)` lets `>>` pull out one word at a time, so counting the words in `  one two   three four  ` is a four-line loop instead of a hand-written walk over the spaces. Runs of spaces cost you nothing, because `>>` skips whitespace before every word."
      ],
      "snippet": "string line, w;\ngetline(cin, line);\nstringstream ss(line);\nint words = 0;\nwhile(ss >> w) words++;   // 4",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "None of your old loops are lost. A string is still a row of characters reached by `s[i]`, so the palindrome check, the vowel count and the reverse you wrote in C all work here with `s.size()` in place of `strlen(s)`. What you gain is a set of ready-made edits - `push_back`, `pop_back`, `insert`, `erase`, `replace` - for the jobs that used to need a loop of your own.",
        "Two things still need care. `s[10]` on a five-character string is not checked and can do anything, while `s.at(10)` stops the program with a clear error, so `at` is the safe one when you are unsure. And `size()` is how many characters are stored, while `capacity()` is how much room is already reserved - the second is an implementation detail and is usually larger.",
        "The programs below take these one at a time: the two styles side by side, the four ways to reach a character, the capacity functions, the modifiers, iterators, `getline`, and `stringstream`."
      ],
      "snippet": null,
      "trace": null
    }
  ]
}