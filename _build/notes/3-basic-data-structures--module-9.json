{
  "module_id": "3-basic-data-structures--module-9",
  "title": "Doubly linked list: build, print, insert, delete",
  "overview": "A doubly linked list gives every node two links: `next` to the node after it and `prev` to the node before it. This module builds one by hand, prints it both ways, and writes insert and delete at the head, the tail and any index. The extra `prev` link makes tail deletion O(1) and backward walks easy, but every operation now has two links to keep correct.",
  "concepts": [
    {"term": "Node with two links", "explain": "Each node stores `val`, `next` and `prev`. The constructor sets both links to `NULL` so a new node is not connected to anything yet.", "snippet": "class Node { public: int val; Node* next; Node* prev; };"},
    {"term": "Head and tail", "explain": "`head->prev` and `tail->next` are both `NULL`. Keeping a `tail` pointer lets you start at either end.", "snippet": null},
    {"term": "Print forward and backward", "explain": "Forward: start at `head` and follow `next`. Backward: start at `tail` and follow `prev`. No recursion needed.", "snippet": "for(Node* t = tail; t; t = t->prev) cout << t->val << \" \";"},
    {"term": "Every link has a partner", "explain": "If `A->next == B`, then `B->prev` must be `A`. Each insert or delete must fix both directions, or forward and backward prints will disagree.", "snippet": null},
    {"term": "Insert in the middle: four links", "explain": "To put `newNode` after `tmp`: point `newNode->next` at `tmp->next`, point `tmp->next->prev` back at `newNode`, then `tmp->next = newNode` and `newNode->prev = tmp`.", "snippet": "newNode->next = tmp->next;\ntmp->next->prev = newNode;\ntmp->next = newNode;\nnewNode->prev = tmp;"},
    {"term": "O(1) tail delete", "explain": "Thanks to `prev`, the new tail is simply `tail->prev`; no walk from the head is needed.", "snippet": "tail = tail->prev; delete tail->next; tail->next = NULL;"},
    {"term": "Edge cases decide correctness", "explain": "Index 0 is a head operation; the last position is a tail operation; a one-node list must set both `head` and `tail` to `NULL` when emptied.", "snippet": null}
  ],
  "files": [
    {
      "file": "doubly_linked_list.cpp",
      "title": "Build a doubly linked list by hand",
      "kind": "setup",
      "same_as": null,
      "problem": null,
      "what": "Creates three nodes (10, 20, 30) and wires both `next` and `prev` links manually. It prints nothing; it only shows the structure.",
      "steps": [
        "`head` = 10, `a` = 20, `tail` = 30, all with `NULL` links.",
        "`head->next = a` and `a->prev = head` connect 10 and 20 both ways.",
        "`a->next = tail` and `tail->prev = a` connect 20 and 30 both ways."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "No output — the program only builds the list.",
      "watch_out": null,
      "check": {"q": "How many link assignments are needed to connect three nodes both ways?", "a": "Four: two `next` links and two `prev` links."}
    },
    {
      "file": "doubly_linked_list_p.cpp",
      "title": "Build a doubly linked list (practice copy)",
      "kind": "variant",
      "same_as": "doubly_linked_list.cpp",
      "problem": null,
      "what": "Identical re-typing of the manual setup.",
      "steps": ["Same three nodes and four link assignments."],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "No output.",
      "watch_out": null,
      "check": {"q": "What is `head->prev` in this list?", "a": "`NULL` — nothing comes before the head."}
    },
    {
      "file": "print_doubly_link_list.cpp",
      "title": "Print forward and backward",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Prints the hand-built list from head to tail using `next`, then from tail to head using `prev`.",
      "steps": [
        "`print_forward` starts at `head` and follows `tmp->next` until `NULL`.",
        "`print_backward` starts at `tail` and follows `tmp->prev` until `NULL`.",
        "Each function ends its line with `endl`."
      ],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "`10 20 30` then `30 20 10` — the same nodes read in both directions.",
      "watch_out": null,
      "check": {"q": "What does a singly linked list need to print backward that a doubly linked list does not?", "a": "Recursion (or an extra stack), because it has no `prev` links."}
    },
    {
      "file": "print_doubly_link_list_p.cpp",
      "title": "Print both ways (practice copy)",
      "kind": "variant",
      "same_as": "print_doubly_link_list.cpp",
      "problem": null,
      "what": "Re-typed version with a constructor bug that leaves the links uninitialised.",
      "steps": ["Same two print functions, but `print_forward` no longer ends its line."],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "`10 20 30` followed by an endless run of garbage numbers, until the run times out: the tail's `next` is not `NULL`, so the walk wanders through random memory.",
      "watch_out": "The constructor says `this->next = next; this->prev = prev;` — the members are assigned to themselves and stay garbage. Use `NULL` for both. The missing `cout << endl;` in `print_forward` would also glue the two lines together.",
      "check": {"q": "Which node's link actually caused the endless print?", "a": "`tail->next`, which was never set to `NULL`."}
    },
    {
      "file": "input_doubly_linked_list.cpp",
      "title": "Read a doubly linked list from input (buggy)",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Meant to read values until -1 with `insert_at_tail` and print the list, but `main` exits before printing.",
      "steps": [
        "`insert_at_tail` links the new node after `tail` (`tail->next = newNode`, `newNode->prev = tail`) and moves `tail`.",
        "The loop reads values and appends them.",
        "On -1 the code runs `return;` — this leaves `main` entirely, so `print_forward` is never reached."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "10 20 30 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "Empty output: the program ends as soon as it reads -1. After the fix it prints `10 20 30`.",
      "watch_out": "Use `break;` instead of `return;` inside the loop. (A bare `return;` in `int main` is also not valid C++; the compiler here only accepts it with `-fpermissive`.)",
      "check": {"q": "What is the difference between `break` and `return` inside that loop?", "a": "`break` leaves only the loop; `return` leaves the whole function, skipping everything after it."}
    },
    {
      "file": "input_doubly_linked_list_p.cpp",
      "title": "Read a doubly linked list (practice copy)",
      "kind": "variant",
      "same_as": "input_doubly_linked_list.cpp",
      "problem": null,
      "what": "Identical copy, including the `return;` bug.",
      "steps": ["Same input loop that exits `main` on -1."],
      "complexity": "Time O(n) · Space O(n)",
      "input": "10 20 30 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "Empty output, for the same reason as the original.",
      "watch_out": "Replace `return;` with `break;`.",
      "check": {"q": "Why is `insert_at_tail` O(1) in a doubly linked list with a `tail` pointer?", "a": "It only touches the current tail and the new node — no walking."}
    },
    {
      "file": "insert_at_head_doubly_linked_list.cpp",
      "title": "Insert at the head",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Adds 100 in front of the list 10 20 30.",
      "steps": [
        "If the list is empty, the new node becomes both `head` and `tail`.",
        "Otherwise `newNode->next = head` links forward.",
        "`head->prev = newNode` links the old head back.",
        "`head = newNode` makes it the first node."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "`10 20 30` before, `100 10 20 30` after.",
      "watch_out": null,
      "check": {"q": "Which link is new compared with inserting at the head of a singly list?", "a": "`head->prev = newNode`."}
    },
    {
      "file": "insert_at_head_doubly_linked_list_p.cpp",
      "title": "Insert at the head (practice copy)",
      "kind": "variant",
      "same_as": "insert_at_head_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same code, inserting 5 instead of 100.",
      "steps": ["Same three-link head insert."],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "`10 20 30`, then `5 10 20 30`.",
      "watch_out": null,
      "check": {"q": "After this insert, what is `head->next->prev->val`?", "a": "5 — the new head."}
    },
    {
      "file": "insert_at_tail_doubly_linked_list.cpp",
      "title": "Insert at the tail",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Appends 100 twice to the end of 10 20 30.",
      "steps": [
        "Empty list: the new node is both `head` and `tail`.",
        "Otherwise `tail->next = newNode` and `newNode->prev = tail`.",
        "Move `tail = newNode`."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Three lines: `10 20 30`, `10 20 30 100`, `10 20 30 100 100`.",
      "watch_out": null,
      "check": {"q": "Why must `tail` be passed by reference?", "a": "The function moves `tail` to the new node, and the caller must see that change."}
    },
    {
      "file": "insert_at_tail_doubly_linked_list_p.cpp",
      "title": "Insert at the tail (practice copy)",
      "kind": "variant",
      "same_as": "insert_at_tail_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same code, appending 40 and then 50.",
      "steps": ["Same tail insert."],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "`10 20 30`, `10 20 30 40`, `10 20 30 40 50`.",
      "watch_out": null,
      "check": {"q": "What does `print_backward(tail)` print at the end?", "a": "`50 40 30 20 10`."}
    },
    {
      "file": "insert_at_any_position_doubly_linked_list.cpp",
      "title": "Insert at any index",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Inserts values at index 2, index 0 and index 5, reusing the head and tail inserts for the edge cases.",
      "steps": [
        "`pos == 0` → call `insert_at_head` and return.",
        "Otherwise walk `tmp` to index `pos-1`.",
        "If `tmp` is the last node (`tmp->next == NULL`), call `insert_at_tail`.",
        "Otherwise wire the four links: `newNode->next`, `tmp->next->prev`, `tmp->next`, `newNode->prev`.",
        "On `10 20 30 100 100`, inserting 200 at index 2 gives `10 20 200 30 100 100`."
      ],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Each line shows the list after one operation. Line 4 has 200 at index 2, line 5 has 300 at index 0, and line 6 has 400 at index 5 (after 30).",
      "watch_out": "`newNode` is created before the edge-case checks, so in the head and tail cases it is never used and leaks. Create it after those checks. There is also no check that `pos` is within `0..size`.",
      "check": {"q": "In the middle case, why set `tmp->next->prev` before changing `tmp->next`?", "a": "After `tmp->next = newNode`, you can no longer reach the old next node through `tmp`."}
    },
    {
      "file": "insert_at_any_position_doubly_linked_list_p.cpp",
      "title": "Insert at any index (practice copy)",
      "kind": "variant",
      "same_as": "insert_at_any_position_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same functions with a shorter demo: one tail insert, then 200 at index 2, 500 at index 0 and 600 at index 6 (the end).",
      "steps": ["Index 6 equals the list size, so the walk stops on the last node and `insert_at_tail` is used."],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Last line `500 10 20 200 30 100 600` — 600 went to the very end.",
      "watch_out": null,
      "check": {"q": "For a list of size 6, which insert indexes are valid?", "a": "0 through 6 (6 means append at the tail)."}
    },
    {
      "file": "delete_at_head_doubly_linked_list.cpp",
      "title": "Delete the head",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "After the insert demo, removes the first node twice.",
      "steps": [
        "Empty list → return.",
        "One node → delete it and set `tail = NULL`.",
        "Otherwise save `head`, move `head = head->next`, set `head->prev = NULL`, and delete the saved node."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "The last two lines lose 300 and then 10 from the front: `10 20 200 30 400 100 100`, then `20 200 30 400 100 100`.",
      "watch_out": "In the one-node case `head` is deleted but not set to `NULL`, so it becomes a dangling pointer. Add `head = NULL;` (the practice copy does).",
      "check": {"q": "Why set `head->prev = NULL` after moving `head`?", "a": "Otherwise the new head still points back at the freed node."}
    },
    {
      "file": "delete_at_head_doubly_linked_list_p.cpp",
      "title": "Delete the head (practice copy)",
      "kind": "variant",
      "same_as": "delete_at_head_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same program with the one-node case fixed: it also sets `head = NULL`.",
      "steps": ["Same head delete, now safe when the last node is removed."],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Same lines as the original.",
      "watch_out": null,
      "check": {"q": "When the only node is deleted, what should `head` and `tail` be?", "a": "Both `NULL`."}
    },
    {
      "file": "delete_at_tail_doubly_linked_list.cpp",
      "title": "Delete the tail",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Removes the last node three times, using `prev` to find the new tail instantly.",
      "steps": [
        "Save `tail` in `deleteNode`.",
        "If `head == tail` (one node), delete it and set both to `NULL`.",
        "Otherwise `tail = tail->prev` and `tail->next = NULL`.",
        "Delete the saved node."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "The last three lines lose one value from the end each time: `20 200 30 400 100`, `20 200 30 400`, `20 200 30`.",
      "watch_out": "It does not check for an empty list; calling it when `tail` is `NULL` crashes.",
      "check": {"q": "Why is this O(1) when the singly version was O(n)?", "a": "`tail->prev` gives the second-last node directly."}
    },
    {
      "file": "delete_at_tail_doubly_linked_list_p.cpp",
      "title": "Delete the tail (practice copy)",
      "kind": "variant",
      "same_as": "delete_at_tail_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same result, but it cuts the link first with `tail->prev->next = NULL` and then moves `tail`.",
      "steps": ["Both orders work, as long as `deleteNode` was saved first."],
      "complexity": "Time O(1) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Same lines as the original.",
      "watch_out": null,
      "check": {"q": "Why is it safe to change `tail` before deleting?", "a": "The old tail is still reachable through `deleteNode`."}
    },
    {
      "file": "delete_at_any_position_doubly_linked_list.cpp",
      "title": "Delete at any index (crashes)",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Deletes the node at a given index, using the head and tail deletes for the edge cases. The tail check is wrong, so the demo crashes.",
      "steps": [
        "`pos == 0` → `delete_at_head`.",
        "Walk `tmp` to index `pos-1`.",
        "The code checks `tmp->next == NULL` to decide on a tail delete.",
        "Otherwise unlink: `tmp->next = tmp->next->next`, then `tmp->next->prev = tmp`, and delete the saved node."
      ],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Output stops after `20 200 30` and the program crashes (access violation). Deleting index 2 removes the last node, so `tmp->next` becomes `NULL` and `tmp->next->prev` dereferences `NULL`.",
      "watch_out": "The tail test must look one node further: `if(tmp->next->next == NULL) { delete_at_tail(head, tail); return; }`. The practice copy has exactly this fix.",
      "check": {"q": "When `pos` is the last index, where does `tmp` stop?", "a": "On the second-last node, so `tmp->next` is the tail and `tmp->next->next` is `NULL`."}
    },
    {
      "file": "delete_at_any_position_doubly_linked_list_p.cpp",
      "title": "Delete at any index (fixed practice copy)",
      "kind": "variant",
      "same_as": "delete_at_any_position_doubly_linked_list.cpp",
      "problem": null,
      "what": "Same program with two fixes: the tail case checks `tmp->next->next == NULL`, and the one-node head delete sets `head = NULL`.",
      "steps": ["Deleting the last index now goes to `delete_at_tail`, so no `NULL` is dereferenced."],
      "complexity": "Time O(n) · Space O(1)",
      "input": null,
      "input_note": null,
      "output_note": "Runs to the end: `20 200` (index 2 removed), `20 300 200`, `20 400 300 200`. The final delete has no print after it.",
      "watch_out": null,
      "check": {"q": "What list remains after the last `delete_at_any_position(head, tail, 2)`?", "a": "`20 400 200` — 300 at index 2 is removed."}
    },
    {
      "file": "all_common_operations_doubly_linked_list.cpp",
      "title": "All operations together (buggy main)",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Collects all the doubly linked list functions in one file; `main` is supposed to read a list and print it, but the loop never runs.",
      "steps": [
        "Contains `print_forward`, `print_backward`, the three inserts and the three deletes from this module.",
        "`main` starts with `tail = NULL` and loops `while(tail)`.",
        "Because `tail` is `NULL`, the loop body never runs, and nothing is read.",
        "`print_forward(NULL)` prints just an empty line."
      ],
      "complexity": "Time O(n) for input once fixed · Space O(n)",
      "input": "10 20 30 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "Just a blank line — no values were read. Once fixed it prints `10 20 30`.",
      "watch_out": "Use `while(true)` and `break;` on -1 (the current `return;` would also skip the print). The file also still has the `tmp->next == NULL` tail-check bug in `delete_at_any_position`.",
      "check": {"q": "Why does `while(tail)` never run here?", "a": "`tail` starts as `NULL`, which counts as false."}
    }
  ],
  "practice_links": [
    {"name": "Design Linked List", "url": "https://leetcode.com/problems/design-linked-list/", "site": "LeetCode"},
    {"name": "Design Browser History", "url": "https://leetcode.com/problems/design-browser-history/", "site": "LeetCode"},
    {"name": "Flatten a Multilevel Doubly Linked List", "url": "https://leetcode.com/problems/flatten-a-multilevel-doubly-linked-list/", "site": "LeetCode"},
    {"name": "LRU Cache", "url": "https://leetcode.com/problems/lru-cache/", "site": "LeetCode"}
  ],
  "recap": [
    "A doubly node has `next` and `prev`; set both to `NULL` in the constructor.",
    "Every change must keep `A->next == B` and `B->prev == A` true.",
    "Head and tail insert/delete are all O(1); any-index operations are O(n) because of the walk.",
    "Reuse the head/tail functions for index 0 and the last index.",
    "When the list becomes empty, set both `head` and `tail` to `NULL`."
  ],
  "check_yourself": [
    {"q": "Which operation gets faster when you move from a singly to a doubly linked list with a tail pointer?", "a": "Deleting the tail: O(n) becomes O(1)."},
    {"q": "How can you quickly test that all `prev` links are correct?", "a": "Print the list forward and backward; the backward line should be the exact reverse of the forward line."},
    {"q": "What does a doubly linked list cost in exchange for these benefits?", "a": "An extra pointer in every node (more memory) and more links to update in every operation."}
  ]
}
