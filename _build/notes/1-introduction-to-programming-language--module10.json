{
  "module_id": "1-introduction-to-programming-language--module10",
  "title": "Practice day: arrays and strings",
  "overview": "Module 10 is a practice day that brings together the two tools you have just learned, arrays and strings, and puts them to work on small judge problems. The six solutions here walk an array from both ends, treat a string as a plain character array, and check characters one by one. Everything uses only what the course has taught so far: loops, conditions, arrays and `scanf`/`printf` — no `<string.h>`, no functions of your own.",
  "concepts": [
    {
      "term": "Walking an array backwards",
      "explain": "To print an array in reverse, start the loop at the last index `n-1` and go down to `0`. Nothing needs to move in memory.",
      "snippet": "for(int i = n - 1; i >= 0; i--) printf(\"%d \", a[i]);"
    },
    {
      "term": "Two-pointer palindrome check",
      "explain": "Compare the first element with the last, the second with the second last, and keep moving towards the middle. One mismatch is enough to answer NO, so you can stop early.",
      "snippet": "int ok = 1;\nfor(int i = 0; i < n / 2; i++)\n    if(a[i] != a[n - 1 - i]) ok = 0;"
    },
    {
      "term": "A string is a char array ending in `'\\0'`",
      "explain": "`scanf(\"%s\", s)` fills the array and adds the null character at the end, and it stops at the first space. So the array must have one slot more than the longest word you expect.",
      "snippet": "char s[105];\nscanf(\"%s\", s);"
    },
    {
      "term": "Counting a length by hand",
      "explain": "Before `<string.h>` (Module 11) you find a length by walking the array until the closing `'\\0'` and counting the steps. That loop is all `strlen` does.",
      "snippet": "int length = 0;\nfor(int i = 0; s[i] != '\\0'; i++) length++;"
    },
    {
      "term": "Character arithmetic",
      "explain": "Letters are stored as numbers (ASCII), and a capital letter is exactly 32 less than its small twin. So `s[i] - 32` makes a letter capital and `s[i] + 32` makes it small.",
      "snippet": "if(c >= 'a' && c <= 'z') c = c - 32;"
    },
    {
      "term": "Shortening long words",
      "explain": "When a word is too long, print its first letter, then how many letters sit in the middle (`length - 2`), then its last letter.",
      "snippet": "printf(\"%c%d%c\\n\", s[0], length - 2, s[length - 1]);"
    }
  ],
  "files": [
    {
      "file": "1_reversing.c",
      "title": "Reversing: print an array backwards",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces F - Reversing",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/F"
      },
      "what": "Reads `n` numbers into an array and prints them in the opposite order. The statement forbids built-in functions, and none are needed: printing backwards is just a loop that counts down.",
      "steps": [
        "Read `n`, then fill `a[0] … a[n-1]` with `scanf` inside a loop.",
        "Start a second loop at `i = n - 1`, the last filled index, and run it down while `i >= 0`.",
        "Print `a[i]` followed by a space on each step, so the numbers come out last-to-first.",
        "The array itself is never changed: reversing the *printing* is enough, so no swapping and no second array are needed."
      ],
      "complexity": "Time O(n) · Space O(n) for the array",
      "input": "4\n5 1 3 2\n",
      "input_note": "The first example from the statement: `n = 4`, then the four values.",
      "output_note": "`2 3 1 5` — the input read right to left. The trailing space after the last number is fine; judges ignore spaces at the end of a line.",
      "watch_out": "Size the array by the constraint, not by the sample. `N` can reach 1000, so `a[1005]` is declared once and works for every test.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why does the loop start at `n - 1` instead of `n`?",
        "a": "`n` values are stored at indices `0` to `n-1`, so `a[n]` is past the end. Starting at `n - 1` prints the last value first."
      }
    },
    {
      "file": "2_palindrome_array.c",
      "title": "Palindrome Array: does it read the same both ways?",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces G - Palindrome Array",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/G"
      },
      "what": "Prints YES when the array reads the same forwards and backwards, otherwise NO. It compares matching pairs from the two ends and stops as soon as a pair disagrees.",
      "steps": [
        "Read `n` and fill the array.",
        "Set a flag `is_palindrome = 1`, meaning \"still a palindrome so far\".",
        "Loop `i` from `0` while `i < n / 2`, comparing `a[i]` with its mirror `a[n - 1 - i]`.",
        "On the first mismatch set the flag to `0` and `break`: no later pair can repair it.",
        "Print YES when the flag survived, otherwise NO.",
        "Stopping at `n / 2` is enough because every pair is then checked once. With an odd `n` the middle element is skipped, and that is correct: it is its own mirror."
      ],
      "complexity": "Time O(n) · Space O(n) for the array",
      "input": "5\n1 3 2 3 1\n",
      "input_note": "The first example: five values that mirror around the middle `2`.",
      "output_note": "`YES`. The pairs (1,1) and (3,3) match and the middle `2` needs no partner. The second example `1 2 3 4` prints `NO`, because `1` and `4` differ immediately.",
      "watch_out": "Print exactly `YES` or `NO` in capitals; a judge compares the text character by character, so `Yes` fails.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "With `n = 7`, how many comparisons does the loop make, and which element is never compared?",
        "a": "Three: (0,6), (1,5) and (2,4). The middle element at index 3 is skipped, because it mirrors onto itself."
      }
    },
    {
      "file": "3_way_too_long_words.c",
      "title": "Way Too Long Words: abbreviate words longer than 10",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces F - Way Too Long Words",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/F"
      },
      "what": "For each of `t` words, prints the word unchanged when it is at most 10 characters, and otherwise the abbreviation: first letter, the number of letters in between, last letter.",
      "steps": [
        "Read `t`, then repeat the whole job `t` times inside a loop.",
        "Read one word with `scanf(\"%s\", s)`, which stops at the space or newline and closes the word with `'\\0'`.",
        "Count the length by walking the array until that `'\\0'`.",
        "When the length is more than 10, print `s[0]`, then `length - 2`, then `s[length - 1]`.",
        "Otherwise print the word as it is. Either way end the line with `\\n`, because each test case needs its own line."
      ],
      "complexity": "Time O(total characters) · Space O(1) beyond the word",
      "input": "4\nword\nlocalization\ninternationalization\npneumonoultramicroscopicsilicovolcanoconiosis\n",
      "input_note": "The example from the statement: four words, one per line.",
      "output_note": "`word` is short, so it is printed unchanged. `localization` has 12 letters, giving `l` + 10 + `n` = `l10n`; `internationalization` gives `i18n`; the 45-letter word gives `p43s`.",
      "watch_out": "\"Too long\" means strictly more than 10, so a word of exactly 10 characters is printed unchanged. Using `>=` here is the classic wrong answer.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why is the middle number `length - 2` and not `length`?",
        "a": "The first and last characters are printed as letters, so only the `length - 2` characters between them are counted."
      }
    },
    {
      "file": "4_conversion.c",
      "title": "Conversion: swap letter case and turn commas into spaces",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces G - Conversion",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/G"
      },
      "what": "Reads one string and prints it character by character: every comma becomes a space, every small letter becomes capital and every capital becomes small.",
      "steps": [
        "Read the string with `scanf(\"%s\", s)`. This is safe here because the input holds only letters and commas, never a space.",
        "Walk the array with a loop that runs while `s[i] != '\\0'`.",
        "When the character is a comma, print a space instead.",
        "When it is between `'a'` and `'z'`, print `s[i] - 32`, which is the same letter in capitals.",
        "Otherwise it is a capital letter, so print `s[i] + 32` to make it small.",
        "Nothing is stored back into the array: each character is decided and printed as it is met."
      ],
      "complexity": "Time O(|S|) · Space O(|S|) for the string",
      "input": "happy,NewYear,enjoy\n",
      "input_note": "The example from the statement: one line, letters and commas only.",
      "output_note": "`HAPPY nEWyEAR ENJOY` — both commas became spaces, `happy` became capitals, and in `NewYear` each letter flipped the other way.",
      "watch_out": "`scanf(\"%s\", ...)` stops at the first space, so it only works because this input has no spaces. The spaces appear in the *output*, which is printed one character at a time.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why does subtracting 32 turn `'a'` into `'A'`?",
        "a": "In ASCII the capital letters sit 32 places before the small ones (`'A'` is 65, `'a'` is 97), so the same shift works for every letter."
      }
    },
    {
      "file": "5_palindrome.c",
      "title": "Palindrome: does the word read the same backwards?",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces I - Palindrome",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/I"
      },
      "what": "Reads one lowercase word and prints YES when it reads the same in both directions, otherwise NO. It is the palindrome array check again, this time over the characters of a string.",
      "steps": [
        "Read the word with `scanf(\"%s\", s)`, which closes it with `'\\0'`.",
        "Count the length by walking the array until that `'\\0'`, because `strlen` belongs to Module 11.",
        "Set a flag `is_palindrome = 1` and loop `i` from `0` while `i < length / 2`.",
        "Compare `s[i]` with its mirror `s[length - 1 - i]`; on the first mismatch clear the flag and `break`.",
        "Print YES when the flag survived, otherwise NO. With an odd length the middle letter is skipped, which is right: it mirrors onto itself."
      ],
      "complexity": "Time O(|S|) · Space O(|S|) for the string",
      "input": "abba\n",
      "input_note": "The first example from the statement: one lowercase word on one line.",
      "output_note": "`YES`. The pairs (a,a) and (b,b) match. The second example `icpcassiut` prints `NO`, because `i` and `t` differ at once, and `mam` prints `YES`.",
      "watch_out": "The answer must be capital `YES` or `NO`. Also note `scanf(\"%s\", ...)` would stop at a space — safe here, because the statement guarantees lowercase letters only.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why is comparing up to `length / 2` enough instead of the whole word?",
        "a": "Every pair is seen once by then. Going further would compare the same pairs again, in the other order."
      }
    },
    {
      "file": "6_strings.c",
      "title": "Strings: sizes, joining, and swapping first letters",
      "kind": "problem",
      "same_as": null,
      "problem": {
        "name": "Codeforces D - Strings",
        "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/D"
      },
      "what": "Reads two words and prints three lines: their two lengths, the two words joined together, and the two words again after their first characters have traded places.",
      "steps": [
        "Read both words into `a` and `b` with `scanf(\"%s\", ...)`.",
        "Count each length with its own loop that stops at `'\\0'`, then print them separated by a space.",
        "For the join, print `a` and then `b` with nothing in between: `printf(\"%s%s\\n\", a, b)` gives `A + B` without building a third array.",
        "Swap the first characters through a temporary variable: keep `a[0]` in `temp`, copy `b[0]` into `a[0]`, then put `temp` into `b[0]`.",
        "Print both words again, now separated by a single space."
      ],
      "complexity": "Time O(|A| + |B|) · Space O(|A| + |B|)",
      "input": "abcd\nef\n",
      "input_note": "The example from the statement: `A` on the first line, `B` on the second.",
      "output_note": "`4 2` are the two lengths; `abcdef` is the join; `ebcd af` is the pair after the first letters traded places — `abcd` starts with `e` and `ef` starts with `a`.",
      "watch_out": "The swap needs the temporary variable. Writing `a[0] = b[0]` first would overwrite `a[0]`, and `b[0]` would then be given its own value back.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why can the second line be printed without making a new array?",
        "a": "Concatenation only matters for what is shown. Printing `a` immediately followed by `b` puts the same characters on the line, in the same order."
      }
    }
  ],
  "practice_links": [
    {
      "name": "Reversing",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/F",
      "site": "Codeforces"
    },
    {
      "name": "Palindrome Array",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219774/problem/G",
      "site": "Codeforces"
    },
    {
      "name": "Way Too Long Words",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/F",
      "site": "Codeforces"
    },
    {
      "name": "Conversion",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/G",
      "site": "Codeforces"
    },
    {
      "name": "Palindrome",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/I",
      "site": "Codeforces"
    },
    {
      "name": "Strings (ignore the Note section of the statement)",
      "url": "https://codeforces.com/group/MWSDmqGsZm/contest/219856/problem/D",
      "site": "Codeforces"
    }
  ],
  "recap": [
    "Reverse output needs no swapping: just loop from `n-1` down to `0`.",
    "A palindrome check compares matching pairs from both ends and stops at the middle.",
    "Strings are char arrays that end with `'\\0'`; before Module 10 is over you count a length with a loop, not with `strlen`.",
    "Characters are small numbers: a capital letter is 32 less than its small twin.",
    "Swapping two values always needs a temporary variable.",
    "Read the output format closely: spaces, new lines and case all matter on a judge."
  ],
  "check_yourself": [
    {
      "q": "For `n = 6`, which index pairs does a palindrome check compare?",
      "a": "(0,5), (1,4) and (2,3). The loop stops once `i` reaches `n / 2`, when every pair has been seen."
    },
    {
      "q": "What does `'d' - 'a'` evaluate to, and why is that useful?",
      "a": "3. It turns a letter into a 0-based position, which you can use as an array index."
    },
    {
      "q": "Why must a string array be at least one longer than the longest word?",
      "a": "The extra slot holds the `'\\0'` that marks the end of the string."
    },
    {
      "q": "`scanf(\"%s\", s)` reads `happy new year` into `s`. What does `s` hold?",
      "a": "Only `happy`. `%s` stops at the first space, which is why it works for the Conversion problem, where the input has no spaces."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "Put four cards on a table with a number written on each, and write the positions underneath. That row of cards is an array: `a[0]` is the first card and `a[3]` is the last one.",
        "The problems on this day ask two plain things about such a row. Read it out backwards. And say whether it reads the same in both directions. Neither of them needs you to move a single card."
      ],
      "snippet": null,
      "trace": "position   0   1   2   3\ncard       5   1   3   2"
    },
    {
      "heading": null,
      "body": [
        "Take the first question. You might think of rebuilding the row, swapping the first card with the last and so on. You do not have to. Nothing about the row changes, only the order in which you say the cards out loud, so the loop counts down instead of up.",
        "It starts at the last filled position, `n - 1`, and runs while `i` is at least `0`. Four cards sit at positions 0 to 3, which is why it starts at `n - 1` and not `n`: `a[4]` is past the end."
      ],
      "snippet": "for(int i = n - 1; i >= 0; i--){\n    printf(\"%d \", a[i]);\n}",
      "trace": "i = 3  ->  print a[3] = 2\ni = 2  ->  print a[2] = 3\ni = 1  ->  print a[1] = 1\ni = 0  ->  print a[0] = 5\n\nprinted:  2 3 1 5"
    },
    {
      "heading": null,
      "body": [
        "The second question is a two-finger check. Put one finger on the first card and one on the last. If they differ, stop there: nothing in the middle can repair it. If they match, move both fingers one step inwards and look again. Here are the fingers on the row `1 3 2 3 1`.",
        "In code the left finger is `i` and the right one is `n - 1 - i`. They meet after `n / 2` steps, so that is where the loop stops. With an odd `n` the middle card is never compared, which is right: it is its own mirror. One mismatch settles the answer, so keep a flag that starts at 1, clear it on the first bad pair and `break`."
      ],
      "snippet": null,
      "trace": "1   3   2   3   1\n^               ^    1 and 1 match\n    ^       ^        3 and 3 match\n        ^            middle: no partner"
    },
    {
      "heading": null,
      "body": [
        "Now the new part of the day. Rub out the numbers and write a letter on each card, and the row becomes a string. In C a string is nothing more than a `char` array: `s[0]` is the first letter, `s[1]` the second, and the same loops walk it.",
        "One thing is different, and the whole day rests on it. With numbers you read `n` first, so you know how many there are. With a word nobody tells you. Instead `scanf(\"%s\", s)` writes the letters into the array and adds one extra character you never see, the null character `'\\0'`. That marker is the word saying where it ends.",
        "So you find a length by walking until the marker and counting the steps: `int length = 0;` then `for(int i = 0; s[i] != '\\0'; i++) length++;`. The library has a ready-made version of that loop, `strlen`, but it lives in `<string.h>`, which is the next module."
      ],
      "snippet": null,
      "trace": "position  0    1    2    3     4\ns[i]      a    b    b    a    \\0\n                               ^\n                    the hidden end marker"
    },
    {
      "heading": null,
      "body": [
        "Once you can walk a word, the rest of the day is that same loop wearing different clothes. **Is the word a palindrome?** The two-finger check, now on `s[i]` and `s[length - 1 - i]`, so `abba` gives YES and `icpcassiut` gives NO. **Is it too long?** Print `s[0]`, then `length - 2`, then `s[length - 1]`, which turns `localization` into `l10n`. **Swap upper and lower case?** Letters are stored as numbers, and a capital sits exactly 32 below its small twin, so `s[i] - 32` makes a letter capital and `s[i] + 32` makes it small.",
        "Each of these reads the word once from left to right, so even a 100000-character line costs about 100000 steps and finishes instantly, with no second array and no trick. Two things trip people up: the array needs one slot more than the longest possible word, for the `'\\0'`, so size it from the constraint and not from the sample; and `scanf(\"%s\", s)` stops at the first space, so it reads one word, never a whole line.",
        "The six programs below apply these patterns to the judge problems, each with its input and its output explained."
      ],
      "snippet": null,
      "trace": null
    }
  ]
}