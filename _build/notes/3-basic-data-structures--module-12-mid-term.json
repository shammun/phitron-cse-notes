{
  "module_id": "3-basic-data-structures--module-12-mid-term",
  "title": "Mid-term warm-up: C basics (HackerRank style)",
  "overview": "These five short C programs are HackerRank-style exam problems on the fundamentals: nested loops, if/else ranges, counting, running sums and string comparison. They are quick to write but easy to get slightly wrong, so they are good for checking that input reading, boundaries and output format are all right.",
  "concepts": [
    {"term": "Read into an array, then process", "explain": "Read all `N` values with `scanf` into an array first, then loop again to compute the answer. Keeping reading and logic apart makes each loop simple."},
    {"term": "Inclusive ranges", "explain": "\"Between X and Y\" usually includes both ends, so the test is `p >= X && p <= Y`. Off-by-one mistakes at the edges are the most common wrong answer."},
    {"term": "Greedy running sum", "explain": "When items are sorted from smallest to largest, taking them in order and stopping when the total goes over the limit gives the maximum count.", "snippet": "total += times[i];\nif(total <= T) count++;\nelse break;"},
    {"term": "Comparing C strings", "explain": "`strcmp(a, b)` returns a negative number if `a` comes first in dictionary order, 0 if equal, positive if later. Copy strings with `strcpy`, never with `=`."},
    {"term": "Use `long long` for big totals", "explain": "A sum of up to 10^5 values can pass the `int` limit (about 2.1 × 10^9). Use `long long` with `%lld`."}
  ],
  "files": [
    {
      "file": "do_it.c",
      "title": "Do it: print 1..K, N times",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Prints the numbers `1` to `K` on one line, and repeats that line `N` times, using two nested loops.",
      "steps": [
        "Read `N` and `K`.",
        "Outer loop runs `N` times — one per line.",
        "Inner loop prints `j` from `1` to `K`, each followed by a space.",
        "After the inner loop, print `\\n` to finish the line."
      ],
      "complexity": "Time O(N·K) · Space O(1)",
      "input": "3 4\n",
      "input_note": "`N` then `K`.",
      "output_note": "Three lines, each `1 2 3 4 ` (with a trailing space, which the judge accepts).",
      "watch_out": "Print the newline in the outer loop, not the inner one; otherwise every number ends up on its own line.",
      "check": {"q": "How many numbers are printed in total for `N = 7`, `K = 4`?", "a": "7 × 4 = 28."}
    },
    {
      "file": "job_application1.c",
      "title": "Job application: classify by experience",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Reads `N` experience values and prints a category for each using an if / else-if chain over ranges.",
      "steps": [
        "Read `N`, then the `N` values into `experiences`.",
        "For each value: `< 1` → Entry-level.",
        "`1..3` → Junior; `4..7` → Mid-level.",
        "Anything else (8 and above) → Senior."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "5\n4 0 3 8 1\n",
      "input_note": "`N`, then `N` experience values (spaces or newlines both work with `scanf`).",
      "output_note": "One line per candidate in input order: Mid-level, Entry-level, Junior, Senior, Junior.",
      "watch_out": "Because the chain is `else if`, the `>= 1` and `>= 4` parts are already guaranteed — they are harmless but not needed. `int experiences[100000]` on the stack is about 400 KB; fine here, but large arrays are safer as globals.",
      "check": {"q": "Which category does experience `7` fall into?", "a": "Mid-level candidate (4 to 7 inclusive)."}
    },
    {
      "file": "price_range.c",
      "title": "Price range: count values in [X, Y]",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Counts how many phone prices fall inside the inclusive range `X..Y`.",
      "steps": [
        "Read `N`, `X`, `Y` (the range as `long long`).",
        "Read the `N` prices into `prices`.",
        "For each price, add 1 to `count` if `prices[i] >= X && prices[i] <= Y`.",
        "Print `count`."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "10 4 8\n8 7 2 3 1 10 25 8 13 5\n",
      "input_note": "`N X Y`, then `N` prices. This is the problem's own sample (expected `4`).",
      "output_note": "`4` — the prices 8, 7, 8 and 5 are inside 4..8. Both 8s count because the range is inclusive.",
      "watch_out": "`long long prices[100000]` is about 800 KB of stack memory; move it outside `main` if you ever see a crash on big input.",
      "check": {"q": "If the range were exclusive, what would the answer be for the sample?", "a": "`2` — only 7 and 5 are strictly between 4 and 8."}
    },
    {
      "file": "book_reading.c",
      "title": "Book reading: how many books fit in T",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "The reading times are sorted, so reading the shortest books first gives the most books. The code keeps a running total and stops once it exceeds `T`.",
      "steps": [
        "Read `N`, `T` and the sorted `times`.",
        "Add each time to `total_time` (a `long long`).",
        "If the total is still `<= T`, count the book.",
        "Otherwise `break` — no later (longer) book can fit either.",
        "Print `number_of_books`."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "10 33\n1 3 4 6 8 10 12 15 23 36\n",
      "input_note": "`N T`, then `N` times in increasing order. This is the problem's sample (expected `6`).",
      "output_note": "`6` — 1+3+4+6+8+10 = 32 fits in 33; adding 12 makes 44, which is too much.",
      "watch_out": "`total_time` must be `long long`: 10^5 books of large times can overflow `int`.",
      "check": {"q": "Why is it safe to `break` at the first book that doesn't fit?", "a": "The list is increasing, so every later book is at least as long and cannot fit either."}
    },
    {
      "file": "lexic_min_max.c",
      "title": "Lexicographic min and max of 3 strings",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Finds the dictionary-smallest and dictionary-largest of three words using `strcmp` and `strcpy`.",
      "steps": [
        "Read `S1`, `S2`, `S3` with `scanf(\"%s\")`.",
        "Start both `min` and `max` as copies of `S1`.",
        "If `strcmp(S2, min) < 0` or `strcmp(S3, min) < 0`, copy that string into `min`.",
        "Likewise update `max` when `strcmp(..., max) > 0`.",
        "Print `min` then `max` on separate lines."
      ],
      "complexity": "Time O(L) where L is the string length · Space O(L)",
      "input": "banana apple cherry\n",
      "input_note": "Three lowercase words on one line.",
      "output_note": "`apple` then `cherry`.",
      "watch_out": "Arrays are size `1001` so a 1000-letter word plus the `\\0` fits. `min = S2` would not compile for arrays — use `strcpy`.",
      "check": {"q": "What is the lexicographic minimum of `ab`, `abc`, `b`?", "a": "`ab` — a prefix comes before the longer word."}
    }
  ],
  "practice_links": [],
  "recap": [
    "Nested loops: outer = rows, inner = items in a row.",
    "Check range edges carefully: inclusive means `>=` and `<=`.",
    "Sorted input + running sum + `break` = greedy maximum count.",
    "`strcmp` for order, `strcpy` for copying C strings.",
    "Use `long long` for sums that can pass 2 × 10^9."
  ],
  "check_yourself": [
    {"q": "What does `strcmp(\"cat\", \"car\")` return: negative, zero or positive?", "a": "Positive, because `t` comes after `r`."},
    {"q": "In the book problem, what goes wrong if the times were not sorted?", "a": "Taking books in the given order may stop early on a long book while shorter ones later could still fit, so the count would not be the maximum."},
    {"q": "In `price_range.c`, what is printed if the input gives `X > Y` (for example `X = 8`, `Y = 4`)?", "a": "`0` — no price can be both `>= 8` and `<= 4`."}
  ]
}
