{
  "module_id": "1-introduction-to-programming-language--module-01",
  "title": "C program structure, data types & input/output",
  "overview": "The first real module covers how a C file is laid out: comments, `#include`, `#define`, global declarations, `main`, and helper functions. It also covers the basic data types and the `printf`/`scanf` format specifiers that go with each type. Getting these right early saves many confusing bugs later.",
  "concepts": [
    {
      "term": "Six sections of a C file",
      "explain": "Documentation comments, then `#include` lines, then `#define` macros, then global declarations, then `main`, then helper functions. Only `main` is required.",
      "snippet": null
    },
    {
      "term": "Preprocessor",
      "explain": "Lines starting with `#` run before compilation. `#include` pastes in a header file. `#define` does a plain find-and-replace.",
      "snippet": "#define PI 3.1416\n#define ll long long"
    },
    {
      "term": "Function prototype",
      "explain": "A declaration like `int sum(int x, int y);` placed above `main` lets `main` call a function whose full body comes later in the file.",
      "snippet": null
    },
    {
      "term": "Data types and sizes",
      "explain": "`short` has 2 bytes, `int` 4 bytes (about ±2.1×10^9), `long long` 8 bytes (about ±9.2×10^18), `char` 1 byte, `float` 4 bytes and `double` 8 bytes. Pick a type big enough for your largest value.",
      "snippet": null
    },
    {
      "term": "Format specifiers",
      "explain": "Each type has its own code: `%d` int, `%hd` short, `%lld` long long, `%c` char, `%f` float, `%lf` double. Using the wrong one gives garbage.",
      "snippet": "scanf(\"%lld\", &x);  printf(\"%.2lf\", d);"
    },
    {
      "term": "Reading a char after a number",
      "explain": "`%c` does not skip whitespace, so it may read the leftover newline. Write `\" %c\"` (with a leading space) to skip it.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "test.c",
      "title": "Hello World (module check)",
      "kind": "setup",
      "same_as": null,
      "problem": null,
      "what": "The same Hello World program as the course root. It confirms the setup works in this module folder.",
      "steps": [
        "Include `<stdio.h>`.",
        "Print `Hello World!` with a newline.",
        "Return 0."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "The program prints `Hello World!`.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Which header file declares `printf`?",
        "a": "`<stdio.h>`"
      }
    },
    {
      "file": "basic.c",
      "title": "Skeleton of a C program",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "An annotated outline of a C file's sections: comments, includes, defines, a global variable, a prototype, an empty `main`, and a `sum` function.",
      "steps": [
        "Multi-line (`/* */`) and single-line (`//`) comments are ignored by the compiler.",
        "`#include <stdio.h>` and `<math.h>` bring in the input/output and math libraries.",
        "`#define pi 3.14` creates a constant by text replacement.",
        "`int N = 100;` is a global variable, and `int sum(int x, int y);` is a prototype.",
        "`main` just returns 0, and `sum` is defined after it."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "The program prints nothing. It only shows the layout of a C file.",
      "watch_out": "`#define long long ll` is backwards: it makes a macro named `long`. It only compiles because nothing uses it. The intended line is `#define ll long long` (macro name first).",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why is the prototype `int sum(int x, int y);` needed if `sum` is written below `main`?",
        "a": "The compiler reads the file from top to bottom. The prototype tells it about `sum` before any call to it appears."
      }
    },
    {
      "file": "c_program_structure.c",
      "title": "Program structure, fully explained",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "A polished version of basic.c. Each of the six sections has a comment block explaining it, and `main` now does something visible: it calls `sum` and uses `PI`, the `ll` macro and `sqrt`.",
      "steps": [
        "`#define PI 3.1416` and `#define ll long long` are macros (text replacement, no memory used).",
        "Global `N = 100` and the prototype of `sum` come before `main`.",
        "`main` computes `sum(7, 5)`, `PI * N`, and `big = 10^9 * 10^9` stored in a `long long`.",
        "`sqrt(2.0)` comes from `<math.h>`.",
        "Each result is printed with its matching specifier: `%d`, `%f`, `%lld`."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "You will see `sum(7, 5) = 12`, `PI * N = 314.160000` (`%f` shows 6 decimals), `big = 1000000000000000000` (10^18), and `sqrt(2) = 1.414214`.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why must `big` be a `long long` rather than an `int`?",
        "a": "10^18 is far above the `int` limit of about 2.1×10^9. It fits only in 64 bits."
      }
    },
    {
      "file": "data_types_variables.c",
      "title": "Data types & variables (empty)",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "This file is empty. It was a placeholder for the data-types lesson; the reference sheet for this module covers that topic.",
      "steps": [
        "There is no code to walk through. For the topic itself, see the concepts above and input_output.c."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "There is no output: the build fails because the file has no `main` function.",
      "watch_out": "The build fails with `undefined reference to WinMain`, the linker's way of saying there is no `main`. Fix: add `int main() { return 0; }` and your variable examples.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "What is the one function every C program must have?",
        "a": "`main`"
      }
    },
    {
      "file": "input_output.c",
      "title": "scanf / printf with many types",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Reads several ints, a short, a char, a float and a double, then prints them back. It shows one format specifier for each type.",
      "steps": [
        "`scanf(\"%d %d %d %hd\", ...)` reads three `int` values and a `short` (`%hd`).",
        "`scanf(\"\\n%c\", &ch)` first skips the leftover newline, then reads one character.",
        "`scanf(\"\\n%.2f\", &f)` is meant to read a float, but a precision like `.2` is not allowed in `scanf` (see Watch out).",
        "`scanf(\"%lf\", &g)` reads a double.",
        "The two `printf` calls print the values, including `e`, a short that was set to -32 in the code."
      ],
      "complexity": null,
      "input": "10 20 30 -5\nZ\n3.75\n2.5\n",
      "input_note": "Line 1: three ints and a short. Line 2: a char. Line 3: a float. Line 4: a double.",
      "output_note": "The ints, the short and the char print correctly. But `f = 0.000000` and `g = 3.750000`: the float was never read, so its value is junk (here 0), and `3.75` went into `g`. The stray `\\n, f =` appears because the newline is inside the first format string.",
      "watch_out": "`%.2f` is not valid in `scanf`, so the read stops and `f` stays uninitialized. Every later read shifts by one. Fix: `scanf(\"%f\", &f);`. Use `.2` only in `printf`, like `printf(\"%.2f\", f)`.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Which specifier reads a `short int` and which prints a `double`?",
        "a": "`%hd` reads a short. `%lf` (or `%f`) prints a double."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "A C file has six sections, and only `main` is required.",
    "`#define` is text replacement: no type, no memory, no semicolon.",
    "Choose the type by range: `int` goes to about 2×10^9, `long long` to about 9×10^18.",
    "Match every variable with its specifier: `%d`, `%hd`, `%lld`, `%c`, `%f`, `%lf`.",
    "Precision like `%.2f` is only for `printf`, never `scanf`.",
    "Put a space before `%c` (`\" %c\"`) to skip leftover newlines."
  ],
  "check_yourself": [
    {
      "q": "Which type would you use for a value up to 10^15, and which specifier reads it?",
      "a": "`long long`, read with `%lld`."
    },
    {
      "q": "What does `#define ll long long` do?",
      "a": "Before compiling, every `ll` in the code is replaced with `long long`."
    },
    {
      "q": "Why can `scanf(\"%c\", &ch)` read a newline by mistake after `scanf(\"%d\", &n)`?",
      "a": "`%d` leaves the Enter key's newline behind, and `%c` does not skip whitespace. Use `\" %c\"` to skip it."
    },
    {
      "q": "What does `return 0;` from `main` tell the operating system?",
      "a": "That the program ended successfully."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "Write a set of instructions on a card for someone who will follow them exactly, in the order you wrote them, and who will never guess what you meant. Step one, then step two, then step three. A program is that card, and the computer is that helper: very fast, very patient, and completely literal.",
        "A C program is a plain text file. You write the steps, a tool called the compiler turns them into something the machine can run, and then they run from top to bottom. The shortest one that does anything visible is this:"
      ],
      "snippet": "#include <stdio.h>\n\nint main() {\n    printf(\"Hello World!\\n\");\n    return 0;\n}",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Every line there has a job. `#include <stdio.h>` is a borrowing line: C by itself cannot even print, because printing lives in a library, and that line brings it in. `int main()` is where the computer starts, and the curly braces hold the steps. `printf` sends text to the screen, `\\n` means move to a new line, and `return 0;` tells the operating system that the program finished without trouble.",
        "A full C file has room for six kinds of things, and they always come in this order:"
      ],
      "snippet": null,
      "trace": "1  comments     notes for humans, ignored by C\n2  #include     borrow printf, sqrt and friends\n3  #define      a short name for a fixed value\n4  globals      variables and function promises\n5  main         where the computer starts\n6  functions    the bodies promised in step 4"
    },
    {
      "heading": null,
      "body": [
        "Only `main` is required. The other five are there when you need them, and `basic.c` and `c_program_structure.c` on this page show all six in a single file.",
        "The second half of the module is about holding values. A variable is a box with a name, and the box holds one value at a time. `int age = 20;` asks for a box called `age` and puts 20 in it. A later `age = 21;` throws the old value away and puts 21 there instead.",
        "The word in front of the name says how big the box is, and that decides the largest value it can hold:"
      ],
      "snippet": null,
      "trace": "type        size   biggest value      code\nchar        1 B    one character      %c\nshort       2 B    about 32000        %hd\nint         4 B    about 2.1 x 10^9   %d\nlong long   8 B    about 9.2 x 10^18  %lld\nfloat       4 B    about 6 digits     %f\ndouble      8 B    about 15 digits    %lf"
    },
    {
      "heading": null,
      "body": [
        "Choose the type by the biggest value you will ever put in the box. A person's age fits in a `short`. The number of people in a large city, say 21000000, needs an `int`. But 10^9 multiplied by 10^9 is 10^18, far past the `int` limit of about 2.1 x 10^9, so that needs a `long long`. Picking a box that is too small does not give an error; it quietly gives a wrong number.",
        "The other new thing is that a program can now take values from **you** instead of only printing fixed text. `scanf` reads and `printf` writes, and both need the code from the last column so they know which kind of box is involved:"
      ],
      "snippet": "int n;\ndouble d;\n\nscanf(\"%d\", &n);            // read a whole number\nscanf(\"%lf\", &d);           // read a decimal number\nprintf(\"n = %d\\n\", n);\nprintf(\"d = %.2lf\\n\", d);    // two decimal places",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "The `&` in front of a name in `scanf` means **where that box is**. `scanf` has to put a value into the box, so it needs the address. `printf` only reads the value, so it does not.",
        "Get a code wrong and nothing warns you. Reading an `int` with `%lf`, or printing a `long long` with `%d`, simply prints rubbish. Two traps catch everybody. A precision like `%.2f` belongs in `printf` only; in `scanf` it is not allowed, the read stops there, and every later read shifts by one. And `%c` does not skip whitespace, so after reading a number it picks up the newline from the Enter key you pressed. Writing `\" %c\"`, with a space in front, skips that newline.",
        "The programs below go through all of this in order: `test.c` checks that your setup works, `basic.c` and `c_program_structure.c` lay out the six sections, and `input_output.c` reads many types at once and shows exactly what the output looks like when one specifier is wrong."
      ],
      "snippet": null,
      "trace": null
    }
  ]
}