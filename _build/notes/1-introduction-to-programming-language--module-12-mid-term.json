{
  "module_id": "1-introduction-to-programming-language--module-12-mid-term",
  "title": "Mid-term exam: loops, conditions, arrays and strings",
  "overview": "These are Shammunul's answers to the mid-term exam problems. Each one is short, but together they test everything up to this point: nested loops, `if / else if` chains, reading arrays, counting with a condition, stopping early with `break`, and comparing strings with `strcmp`. They are good for revision because each one uses a single clear pattern.",
  "concepts": [
    {"term": "Read first, then process", "explain": "Most solutions read all `N` values into an array in one loop, then work on them in a second loop. Keeping the two jobs apart makes the logic easier to follow.", "snippet": null},
    {"term": "Counting with a condition", "explain": "Start a counter at 0 and add 1 each time a value passes the test. A range test has two sides: `x >= X && x <= Y`.", "snippet": "if(p[i] >= X && p[i] <= Y) count++;"},
    {"term": "Running total with an early stop", "explain": "Keep adding values to a total. The first time the total goes past the limit, `break`, because the list is sorted and later values are even bigger.", "snippet": null},
    {"term": "`if / else if` ranges", "explain": "Check the ranges in order so that each value falls into exactly one branch. Put the catch-all case in the final `else`.", "snippet": null},
    {"term": "Min and max of strings", "explain": "Strings can't be compared with `<`. Use `strcmp`, and use `strcpy` to save the current best string.", "snippet": "if(strcmp(s2, mn) < 0) strcpy(mn, s2);"},
    {"term": "Use `long long` for big values", "explain": "Limits such as `10^9`, or sums of many values, can overflow a 32-bit `int`. Use `long long` with `%lld`.", "snippet": null}
  ],
  "files": [
    {
      "file": "do_it.c",
      "title": "Print 1..K, N times",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Prints the row `1 2 ... K` a total of `N` times. It is a basic nested loop.",
      "steps": [
        "Read `N` (how many rows) and `K` (how far each row counts).",
        "The outer loop `i` runs `N` times, once for each row.",
        "The inner loop `j` goes from 1 to `K` and prints each number followed by a space.",
        "After the inner loop, `printf(\"\\n\")` ends the row."
      ],
      "complexity": "Time O(N·K) · Space O(1)",
      "input": "3 4\n",
      "input_note": "N = 3 rows, K = 4 numbers per row.",
      "output_note": "Three lines of `1 2 3 4 `. Each line ends with a space, which judges usually accept.",
      "watch_out": "Put the newline outside the inner loop. Inside it, every number would land on its own line.",
      "check": {"q": "How many numbers are printed in total for input `7 4`?", "a": "28 (7 rows × 4 numbers)."}
    },
    {
      "file": "job_application1.c",
      "title": "Classify candidates by experience",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "For each experience value, prints one of four labels. It is an `if / else if` chain over number ranges.",
      "steps": [
        "Read `N`, then `N` experience values into `experiences`.",
        "For each value: below 1 is `Entry-level`.",
        "From 1 to 3 is `Junior`, and from 4 to 7 is `Mid-level`.",
        "Anything left (above 7) falls into the final `else` and is `Senior`."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "5\n0 2 5 8 3\n",
      "input_note": "5 candidates with experience 0, 2, 5, 8 and 3 years.",
      "output_note": "One label per line, in input order: Entry-level, Junior, Mid-level, Senior, Junior.",
      "watch_out": "The `>= 1` and `>= 4` checks aren't needed, because the earlier branches already ruled those values out. They are harmless, though.",
      "check": {"q": "Which label does experience `7` get?", "a": "Mid-level candidate, because 4 to 7 includes 7."}
    },
    {
      "file": "lexic_min_max.c",
      "title": "Lexicographic min and max of 3 strings",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Finds the string that comes first and the one that comes last in dictionary order. It uses the same pattern as min/max of three numbers, with `strcmp` in place of `<`.",
      "steps": [
        "Read the three words into `S1`, `S2` and `S3`.",
        "Copy `S1` into both `min` and `max` as the starting guess.",
        "If `strcmp(S2, min) < 0`, then `S2` comes earlier, so copy it into `min`. Do the same check for `S3`.",
        "If `strcmp(S2, max) > 0`, then `S2` comes later, so copy it into `max`. Do the same check for `S3`.",
        "Print `min`, then `max`, each on its own line."
      ],
      "complexity": "Time O(L) where L is string length · Space O(L)",
      "input": "mango apple zebra\n",
      "input_note": "Three lowercase words on one line.",
      "output_note": "`apple` (the earliest word), then `zebra` (the latest word).",
      "watch_out": "`min = S2` does not compile for arrays. Use `strcpy`. The arrays are sized 1001 to leave room for `'\\0'`.",
      "check": {"q": "What does `strcmp(\"cat\", \"car\")` return: a negative number, zero or a positive number?", "a": "A positive number, because `t` comes after `r`."}
    },
    {
      "file": "price_range.c",
      "title": "Count prices inside a range",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Counts the phones whose price is between `X` and `Y`, including both ends.",
      "steps": [
        "Read `N`, `X` and `Y`. `X` and `Y` are `long long` because prices can reach 10^9.",
        "Read the `N` prices into `prices`.",
        "Loop over them and add 1 to `count` when `prices[i] >= X && prices[i] <= Y`.",
        "Print `count`."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "6 10 20\n5 10 15 20 25 12\n",
      "input_note": "6 prices, and the range is 10 to 20.",
      "output_note": "`4`: the prices 10, 15, 20 and 12 are inside the range. Both ends count.",
      "watch_out": "`long long prices[100000]` takes about 800 KB inside `main`. It fits here, but making big arrays global is safer.",
      "check": {"q": "What would the answer be if the check used `>` and `<` instead?", "a": "2 (only 15 and 12), because the prices 10 and 20 would be left out."}
    },
    {
      "file": "book_reading.c",
      "title": "How many books fit in T time",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "The reading times are sorted from shortest to longest, so reading the shortest books first gives the most books. The program keeps a running total and stops at the first book that doesn't fit.",
      "steps": [
        "Read `N`, `T` and the `N` sorted times.",
        "`total_time` adds up the time of each book in order.",
        "While `total_time <= T`, add 1 to `number_of_books`.",
        "The first time the total goes past `T`, `break`. Every later book is at least as long, so none of them can fit either.",
        "Print `number_of_books`."
      ],
      "complexity": "Time O(N) · Space O(N)",
      "input": "6 20\n2 3 5 7 11 13\n",
      "input_note": "6 books and 20 minutes. The times are already sorted.",
      "output_note": "`4`: 2+3+5+7 = 17 fits, but adding 11 makes 28, which is more than 20.",
      "watch_out": "`total_time` is `long long`. The sum of 10^5 times can pass the `int` limit.",
      "check": {"q": "With `T = 0`, what does the program print?", "a": "0. The first book already takes more than 0 time (all times are positive), so the loop breaks at once."}
    }
  ],
  "practice_links": [],
  "recap": [
    "Nested loops: the outer loop counts rows, and the inner loop prints what goes in each row.",
    "Range checks use `&&` and must include or exclude the ends exactly as the problem says.",
    "On sorted data, a running total plus `break` answers \"how many fit\".",
    "Compare strings with `strcmp` and copy them with `strcpy`.",
    "Use `long long` when values or sums can pass about 2×10^9."
  ],
  "check_yourself": [
    {"q": "Why does taking the books in the given order give the most books?", "a": "The list is sorted, so the shortest books come first. Any other choice uses at least as much time for the same number of books."},
    {"q": "In an `if / else if` chain, why is the last case usually a plain `else`?", "a": "Everything that didn't match earlier belongs there, so no extra condition is needed."},
    {"q": "Why can't you write `if (s1 < s2)` for two C strings?", "a": "That compares the two array addresses, not the text. Use `strcmp(s1, s2) < 0`."}
  ]
}
