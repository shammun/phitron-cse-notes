{
  "module_id": "software-engineering-track--1-python",
  "title": "First steps in Python",
  "overview": "This is the first folder of the Software Engineering Track and a first Python program. After months of C, Python feels very light: there is no `main`, no `#include`, no semicolons and no compiling. You write a line and the interpreter runs it from top to bottom.",
  "concepts": [
    {"term": "Interpreted, top to bottom", "explain": "Python reads the file and runs each line in order. There is no compile step and no `main()` function you must write.", "snippet": null},
    {"term": "`print()`", "explain": "`print` writes its arguments and then adds a newline by itself, so you don't need to type `\\n` as you do in C's `printf`.", "snippet": "print(\"Hello World\")"},
    {"term": "Strings in quotes", "explain": "Text goes inside single or double quotes. Both kinds work the same way.", "snippet": "print('Hi')\nprint(\"Hi\")"},
    {"term": "Running a script", "explain": "From a terminal, type `python first.py`. The output shows up right away.", "snippet": null}
  ],
  "files": [
    {
      "file": "first.py",
      "title": "Hello World in Python",
      "kind": "setup",
      "same_as": null,
      "problem": null,
      "what": "The classic first program. It checks that Python is installed and that you can run a script.",
      "steps": [
        "`print(\"Hello World\")` is the only line in the file.",
        "Python passes the string to `print`, which writes it and then a newline.",
        "The script ends when there are no more lines. There is no `return 0`."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "You should see exactly one line: `Hello World`.",
      "watch_out": "Python 3 needs the brackets: `print \"Hello\"` (without them) is Python 2 syntax and raises a SyntaxError.",
      "check": {"q": "What is the C line that does the same thing, and what does Python add for you?", "a": "`printf(\"Hello World\\n\");`. Python's `print` adds the newline for you."}
    }
  ],
  "practice_links": [],
  "recap": [
    "Python runs the file from top to bottom. There is no `main` and no compiling.",
    "`print()` adds a newline at the end by itself.",
    "Strings can use single or double quotes.",
    "Run a script with `python file.py`."
  ],
  "check_yourself": [
    {"q": "Name two things a C Hello World needs that the Python one does not.", "a": "Any two of these: `#include <stdio.h>`, a `main` function, semicolons, `return 0`, a compile step."},
    {"q": "How many lines does `print(\"a\"); print(\"b\")` print?", "a": "Two, because each `print` ends with its own newline."}
  ]
}
