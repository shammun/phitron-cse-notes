{
  "module_id": "1-introduction-to-programming-language--course-root",
  "title": "Getting started: compiler & first programs",
  "overview": "Before writing any real C, you need a working toolchain: a C compiler (GCC from MinGW on Windows), an editor (VS Code) and a quick way to run a file (the Code Runner extension). This page sums up that setup and the two tiny programs used to check that everything works: one that only prints, and one that reads a number.",
  "concepts": [
    {
      "term": "Compiler",
      "explain": "C source code is plain text. A compiler (here `gcc`) turns it into an `.exe` the computer can run. You compile first, then run.",
      "snippet": "gcc test.c -o test.exe\n./test.exe"
    },
    {
      "term": "PATH environment variable",
      "explain": "Windows only finds `gcc` from any folder if the compiler's `bin` folder is added to the `Path` system variable. Check it by opening a terminal and typing `gcc -v`; a version message means it worked.",
      "snippet": null
    },
    {
      "term": "VS Code + Code Runner",
      "explain": "Code Runner adds a Run button that saves the file, compiles it and runs it in one click. Its `executorMap` setting (in `settings.json`) holds the exact compile-and-run command for each language.",
      "snippet": null
    },
    {
      "term": "Run in the terminal",
      "explain": "Set `code-runner.runInTerminal` to `true`. The Output panel is read-only, so a program that calls `scanf` can only receive your typing inside the terminal.",
      "snippet": null
    },
    {
      "term": "Online compiler as a backup",
      "explain": "If your local setup is broken, an online compiler such as OnlineGDB lets you keep practising while you fix it.",
      "snippet": null
    },
    {
      "term": "First two sanity checks",
      "explain": "A print-only program proves that compiling and running work. A program that reads input proves that typing into the terminal works too.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "test.c",
      "title": "Hello World",
      "kind": "setup",
      "same_as": null,
      "problem": null,
      "what": "The smallest useful C program: it prints one line. If this runs, your compiler and editor are set up correctly.",
      "steps": [
        "`#include <stdio.h>` brings in `printf`.",
        "`main` is where the program starts running.",
        "`printf(\"Hello World!\\n\")` prints the text; `\\n` moves to a new line.",
        "`return 0;` tells the operating system the program finished without errors."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "You should see exactly `Hello World!` on one line.",
      "watch_out": "If the terminal says `gcc` is not recognized, the MinGW `bin` folder is not in your `Path` yet.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "What does `return 0;` at the end of `main` mean?",
        "a": "The program finished successfully. A non-zero value would signal an error."
      }
    },
    {
      "file": "test2.c",
      "title": "Read one number and print it",
      "kind": "setup",
      "same_as": null,
      "problem": null,
      "what": "Reads one integer with `scanf` and prints it back. It checks that keyboard input reaches your program.",
      "steps": [
        "Declare `int a;` to hold the number.",
        "`scanf(\"%d\", &a)` waits for you to type an integer. `&a` gives `scanf` the address where it should store the value.",
        "`printf(\"a = %d\", a)` replaces `%d` with the value of `a`."
      ],
      "complexity": null,
      "input": "42\n",
      "input_note": "One integer.",
      "output_note": "With input `42` the program prints `a = 42`. There is no `\\n` at the end, so your terminal prompt may appear on the same line.",
      "watch_out": "If the program seems stuck, it is waiting for input. Type a number and press Enter. With Code Runner this only works when `runInTerminal` is on.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why do we write `&a` in `scanf` but just `a` in `printf`?",
        "a": "`scanf` must change `a`, so it needs the variable's address. `printf` only needs its value."
      }
    }
  ],
  "practice_links": [
    {
      "name": "VS Code download",
      "url": "https://code.visualstudio.com/download",
      "site": "Visual Studio Code"
    },
    {
      "name": "Git for Windows (Git Bash)",
      "url": "https://git-scm.com/downloads",
      "site": "git-scm.com"
    },
    {
      "name": "OnlineGDB online C compiler",
      "url": "https://www.onlinegdb.com/online_c_compiler",
      "site": "OnlineGDB"
    }
  ],
  "recap": [
    "The workflow is always the same: write, compile with `gcc`, then run the `.exe`.",
    "Add MinGW's `bin` folder to `Path`, then check with `gcc -v`.",
    "In VS Code, Code Runner plus `runInTerminal: true` lets you run programs that read input.",
    "`printf` writes output and `scanf` reads input. Both come from `<stdio.h>`.",
    "Get Hello World running first, then test input."
  ],
  "check_yourself": [
    {
      "q": "You typed `gcc -v` and got 'command not found'. What is the most likely cause?",
      "a": "The compiler's `bin` folder is not in the `Path` environment variable, or you did not open a new terminal after adding it."
    },
    {
      "q": "Why can't a program that uses `scanf` read your typing in VS Code's Output panel?",
      "a": "The Output panel only shows text; it cannot send keyboard input. Run the program in the integrated terminal instead."
    },
    {
      "q": "What two steps happen when you press Run on a `.c` file?",
      "a": "The file is compiled into an executable, and then that executable is run."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "If you have never compiled anything before, the first hour of C is confusing for a reason that has little to do with C. You write something, press a button, and three separate things happen at once. It is much easier once you pull them apart.",
        "What you write is plain text, nothing more. A file called `test.c` is the same kind of file as a shopping list; you could open it in Notepad. The line `printf(\"Hello World!\\n\");` is a sentence in a language that you and one particular program both understand. The processor inside your computer does not understand it. It follows only a far simpler set of instructions, written as numbers.",
        "A **compiler** is the translator standing between the two. You hand it your text file, it reads the whole thing from top to bottom, and if every line makes sense it writes out a second file holding the same instructions in the form the machine can follow. On Windows that second file ends in `.exe`. The compiler used here is called `gcc`."
      ],
      "snippet": null,
      "trace": "  test.c         gcc          test.exe        run\n ----------  ----------->  -------------  ---------->\n your text    translated     the machine    output on\n             once           can read it    the screen"
    },
    {
      "heading": null,
      "body": [
        "So there are always two steps, never one. First translate, then run:"
      ],
      "snippet": "gcc test.c -o test.exe\n./test.exe",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "In the first line, `test.c` is your text and `-o test.exe` names the file to write. In the second line `gcc` plays no part at all: `test.exe` is now an ordinary file sitting in the same folder, and you could run it again next week without compiling anything.",
        "Two things follow from this, and both catch beginners. If the compiler meets a line it cannot make sense of, it prints an error and writes no `.exe` at all. And if you change the `.c` file but forget to translate it again, running the old `.exe` runs yesterday's version, so it looks as though your edit did nothing. A Run button in an editor is simply something that does both steps for you in one click.",
        "The second thing to picture is how a running program talks to you. It has one stream of text going out to the screen and one coming in from your keyboard. `printf` is the outgoing one: whatever you give it appears in the terminal. `scanf` is the incoming one: the program stops, waits until you type something and press Enter, and stores what you typed in a variable you have set aside for it. The `&` in `scanf(\"%d\", &a)` is how you say which variable to fill."
      ],
      "snippet": null,
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Here is the whole of the second program on this page, `test2.c`. It is four useful lines:"
      ],
      "snippet": "#include <stdio.h>\n\nint main() {\n    int a;\n    scanf(\"%d\", &a);\n    printf(\"a = %d\", a);\n    return 0;\n}",
      "trace": "step                      screen      a\n1 you run test.exe        (nothing)   ?\n2 scanf waits for you     (nothing)   ?\n3 you type 42 and Enter   42          42\n4 printf runs             a = 42      42"
    },
    {
      "heading": null,
      "body": [
        "Line 2 of the trace is the part that surprises people. The window sits there with nothing on it, and it looks as though the program has crashed. It has not; it is waiting. A program that seems frozen right at the start is nearly always stopped at a `scanf`, wanting you to type.",
        "That is also why there are two tiny programs on this page instead of one. The first only prints. If `Hello World!` appears, translating and running both worked, and that is the whole toolchain proved in a single line. The second adds input, which can fail on its own account: some editors show a program's output in a panel that can only display text and cannot take your typing, so `scanf` waits for a key that never arrives.",
        "The notes below cover the setup in order, then go through those two programs line by line. Get the printing one working first, and only then worry about input."
      ],
      "snippet": null,
      "trace": null
    }
  ]
}