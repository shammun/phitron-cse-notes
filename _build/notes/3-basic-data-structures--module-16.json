{
  "module_id": "3-basic-data-structures--module-16",
  "title": "Assignment: stack & queue problems",
  "overview": "This module is an assignment that puts stacks and queues to work. You compare the pop order of a stack and a queue (once with the STL, once with your own linked-list classes), cancel pairs of characters with a stack, and simulate a ticket line with a queue. The big lesson: pick the container whose removal order matches the problem.",
  "intro": [
    {
      "heading": null,
      "body": [
        "This is the assignment day for stacks and queues. No new tool appears. What is being tested is the same small decision as the practice module: **which container removes things in the order the problem wants?**",
        "Push `10 20 30` into both and then empty them. The stack gives back `30 20 10`, because the newest goes first. The queue gives back `10 20 30`, because the oldest goes first. Same input, opposite output. That one difference is what every problem here turns on."
      ],
      "snippet": null,
      "trace": "pushed:        10  20  30\n\nstack empties: 30  20  10   (last in, first out)\nqueue empties: 10  20  30   (first in, first out)"
    },
    {
      "heading": null,
      "body": [
        "**One worked reminder**, and it is worth doing slowly because it is the trap of the day. Two of the problems cancel characters in a binary string with a stack, and they look identical until you read the rules. In one, a `0` may cancel a `1` on the top **and** a `1` may cancel a `0`. In the other, only a `1` may cancel a `0` just before it; a `0` arriving on a `1` cancels nothing.",
        "Take the same string `0110` and run both rules."
      ],
      "snippet": null,
      "trace": "rule A: 0 cancels 1, and 1 cancels 0\n  0   push          stack: 0\n  1   top 0 -> pop   stack: empty\n  1   push          stack: 1\n  0   top 1 -> pop   stack: empty\n  empty  ->  YES\n\nrule B: only 1 cancels a 0\n  0   push          stack: 0\n  1   top 0 -> pop   stack: empty\n  1   push          stack: 1\n  0   no rule, push  stack: 1 0\n  not empty  ->  NO"
    },
    {
      "heading": null,
      "body": [
        "The mechanism is the same in both: the top of the stack is always \"the previous character that has not been cancelled yet\", which is exactly what these problems mean by *previous available*. Only the `if` that decides to pop changes. Copy the rule out of the statement into that `if` before you write anything else.",
        "The queue problem is the other half of the day, and it is a plain simulation: a ticket line where command `0` puts a name at the back and command `1` serves the front. You do not search or sort anything, you act the story out with `push`, `front()` and `pop()`.",
        "One problem asks you to do the comparison twice: once with the STL `stack` and `queue`, and once with classes you write yourself over a linked list. That is the point of it. A stack is a doubly linked list where you push and pop at the tail, both O(1). A queue is a singly linked list where you push at the tail and pop at the head, also both O(1). The STL versions are not magic, only shorter."
      ],
      "snippet": "st.top();    // newest value\nq.front();   // oldest value",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Four things to watch for, and they cost more marks here than the algorithms do.",
        "**Empty containers.** Check `empty()` before every `top()`, `front()` or `pop()`. In the ticket line an empty queue is not a crash, it is a normal case with its own output, and in your hand-written classes popping an empty list would follow a `NULL` pointer.",
        "**Reading the input as it really is.** A command line here is not always the same shape: `0` is followed by a name, `1` is alone on the line. Read the number first, and only then read a name if the number was `0`. If you always read a name you swallow the next command and everything after it is wrong. In the comparison problem, read both lists completely before you decide anything, even when the two sizes already tell you the answer is no.",
        "**Exact output.** `YES` and `NO` in capitals, `Invalid` with one capital letter, one answer per line and nothing extra. A judge compares text, not intent.",
        "The five programs below each show the finished version with its reasoning, its sample input and what to be careful about. Try the problem first, then read the one you got stuck on."
      ],
      "snippet": null,
      "trace": null
    }
  ],
  "concepts": [
    {
      "term": "LIFO vs FIFO",
      "explain": "A stack gives back the **last** value you pushed (LIFO). A queue gives back the **first** value you pushed (FIFO). So pushing the same list into both gives opposite removal orders.",
      "snippet": "st.top();  // newest\nq.front(); // oldest"
    },
    {
      "term": "Compare while popping",
      "explain": "To check if two containers remove items in the same order, look at `top()` and `front()` together, compare, then pop both. Stop at the first mismatch.",
      "snippet": null
    },
    {
      "term": "Stack as a cancel machine",
      "explain": "When a new character can cancel the one just before it, keep the not-yet-cancelled characters on a stack. The top is always the 'previous available' character. If the stack is empty at the end, everything cancelled.",
      "snippet": "if(!st.empty() && st.top()=='0' && c=='1') st.pop();\nelse st.push(c);"
    },
    {
      "term": "Queue as a waiting line",
      "explain": "A ticket line is a queue: join at the back with `push`, leave from the front with `front()` + `pop()`. Always check `empty()` before reading the front.",
      "snippet": null
    },
    {
      "term": "Building your own stack/queue",
      "explain": "A doubly linked list with a `tail` pointer gives O(1) push/pop at the tail (a stack). A singly linked list with `head` and `tail` gives O(1) push at the tail and pop at the head (a queue).",
      "snippet": null
    },
    {
      "term": "Early size check",
      "explain": "If the two lists have different lengths, they can never be the same, so print `NO` right away. Read all input first, then decide.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "asignment_3_problem_1_same_or_not.cpp",
      "title": "Same or not (STL stack and queue)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Pushes `n` values into a `stack` and `m` values into a `queue`, then checks whether popping both gives the same sequence. Uses the STL containers.",
      "steps": [
        "Read `n` and `m`, then push `n` values into `st` and `m` values into `q`.",
        "If `n != m`, the lengths differ, so print `NO` and stop.",
        "While `st` is not empty, compare `st.top()` with `q.front()`.",
        "On any mismatch print `NO` and return; otherwise pop both and continue.",
        "If the loop finishes, every pair matched, so print `YES`."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "4 4\n1 2 3 4\n4 3 2 1\n",
      "input_note": "First line n and m, then the stack values, then the queue values.",
      "output_note": "`YES`: the stack pops 4 3 2 1, and the queue was filled with 4 3 2 1, so it also removes 4 3 2 1.",
      "watch_out": "The size check comes **after** reading both lines on purpose. If you returned before reading, it would not matter here, but in multi-test problems leftover input would break the next case.",
      "check": {
        "q": "If both lines are `1 2 3`, what is printed and why?",
        "a": "`NO`. The stack pops 3 first but the queue removes 1 first."
      }
    },
    {
      "file": "assignment_3_problem_2_same_or_not_2.cpp",
      "title": "Same or not 2 (hand-made stack and queue)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Same task as problem 1, but without the STL: `myStack` (doubly linked list) and `myQueue` (singly linked list) are written by hand and used exactly like the STL ones.",
      "steps": [
        "`Node` holds `val`, `next` and `prev`.",
        "`myStack::push` adds at `tail`; `pop` deletes `tail` and moves `tail` back with `prev`; `top` returns `tail->val`.",
        "`myQueue::push` adds at `tail`; `pop` deletes `head` and moves `head` forward; `front` returns `head->val`.",
        "Both reset the other end to `NULL` when the list becomes empty, so the next push starts fresh.",
        "`main` is the same as problem 1: read, check sizes, compare `top()` with `front()` while popping."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "4 4\n1 2 3 4\n4 3 1 2\n",
      "input_note": "First line n and m, then the stack values, then the queue values.",
      "output_note": "`NO`: the first two pairs match (4 with 4, 3 with 3), but then the stack gives 2 while the queue gives 1.",
      "watch_out": "`myStack::pop` has no empty check. Calling it on an empty stack dereferences `NULL`. Here it is safe because `main` only pops while `!st.empty()`.",
      "check": {
        "q": "Why does the stack need a `prev` pointer but the queue does not?",
        "a": "The stack removes from the tail, so after deleting it must step back to the previous node. The queue removes from the head and only ever moves forward with `next`."
      }
    },
    {
      "file": "assignment_3_problem_3_is_it_valid.cpp",
      "title": "Is it valid? (cancel 01 and 10 pairs)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "A `0` can delete a previous `1` and a `1` can delete a previous `0`. The string is valid if everything gets deleted. A stack keeps the characters still alive.",
      "steps": [
        "Read `t` test cases; for each, read the string `s` and start an empty `stack<char>`.",
        "For every character `c`: if the stack is empty, push it.",
        "Otherwise, if `c` and `st.top()` are different digits, they cancel, so pop.",
        "If they are the same digit, push `c`.",
        "After the loop, an empty stack means `YES`, else `NO`."
      ],
      "complexity": "Time O(|s|) per test · Space O(|s|)",
      "input": "3\n0110\n1001\n0001\n",
      "input_note": "First line t, then one binary string per line.",
      "output_note": "`YES YES NO` (one per line). In `0001` the three 0s pile up and only one of them is cancelled by the final 1.",
      "watch_out": "Because any different pair cancels, the answer is simply 'the number of 0s equals the number of 1s'. The stack still shows the general pattern.",
      "check": {
        "q": "Trace `0110`: what does the stack hold after each character?",
        "a": "`0` → [0]; `1` cancels → []; `1` → [1]; `0` cancels → []. Empty, so YES."
      }
    },
    {
      "file": "assignment_3_problem_4_special_queries.cpp",
      "title": "Special queries (ticket line)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Simulates a ticket counter line with a `queue<string>`. Command `0 name` adds a person; command `1` serves the front person or prints `Invalid` if the line is empty.",
      "steps": [
        "Read the number of queries `n` and create `queue<string> q`.",
        "For each query read `x`.",
        "If `x == 0`, read a name and `push` it to the back.",
        "If `x == 1` and the queue is empty, print `Invalid`.",
        "Otherwise print `q.front()` and `pop` it."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "6\n1\n0 rina\n0 tomal\n1\n1\n1\n",
      "input_note": "First line q, then one command per line.",
      "output_note": "`Invalid`, `rina`, `tomal`, `Invalid`: the first and last `1` find an empty line; the middle two serve people in the order they joined.",
      "watch_out": "Never call `front()` on an empty queue: it is undefined behaviour. The `empty()` check must come first.",
      "check": {
        "q": "Why is a queue (not a stack) right for this problem?",
        "a": "People are served in the order they arrived: first in, first out."
      }
    },
    {
      "file": "assignment_problem_5_elimination.cpp",
      "title": "Elimination (every 1 removes a 0 just before it)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Only the pair `01` (a 0 followed by a 1) gets eliminated, and eliminations keep chaining. A stack handles the chaining automatically.",
      "steps": [
        "For each test, read `s` and start an empty `stack<char>`.",
        "If the stack is empty, push `c` and move on.",
        "If `st.top() == '0'` and `c == '1'`, the pair `01` disappears, so pop.",
        "Otherwise push `c` (this includes a `0` after a `1`, which does **not** cancel).",
        "Empty stack at the end → `YES`, else `NO`."
      ],
      "complexity": "Time O(|s|) per test · Space O(|s|)",
      "input": "3\n0011\n10\n010011\n",
      "input_note": "First line t, then one binary string per line.",
      "output_note": "`YES`, `NO`, `YES`. `10` stays because a 0 cannot remove the 1 before it.",
      "watch_out": "Compare with problem 3: there both `01` and `10` cancel. Here only `01` does, so the order of characters matters, not just the counts.",
      "check": {
        "q": "Does `0101` become empty? Show the stack.",
        "a": "Yes. `0` → [0]; `1` pops → []; `0` → [0]; `1` pops → []."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "Stack = last in, first out; queue = first in, first out.",
    "To compare two containers, check the fronts/tops together and pop both.",
    "A stack is the natural tool for 'cancel with the previous available item' problems, including chain reactions.",
    "Always check `empty()` before `top()`, `front()` or `pop()`.",
    "A hand-made stack and queue are just linked lists with the right end pointers."
  ],
  "check_yourself": [
    {
      "q": "You push 1 2 3 into a stack and 3 2 1 into a queue. Are they 'same'?",
      "a": "Yes. The stack pops 3 2 1 and the queue removes 3 2 1."
    },
    {
      "q": "Why does a stack handle chained eliminations like `0011` without extra loops?",
      "a": "After a pair is removed, the new top is the character that now sits just before the next one, so the next comparison is automatically against it."
    },
    {
      "q": "What is the time cost of push and pop in the linked-list stack and queue?",
      "a": "O(1) each, because they only touch the head or tail pointer."
    }
  ],
  "resources": [
    {
      "kind": "visual",
      "name": "USFCA — queue (array)",
      "url": "https://www.cs.usfca.edu/~galles/visualization/QueueArray.html",
      "note": "Shows front and back moving, and why the array is treated as a circle."
    },
    {
      "kind": "article",
      "name": "GeeksforGeeks — queue",
      "url": "https://www.geeksforgeeks.org/queue-data-structure/",
      "note": "The queue explained, with its standard problems."
    },
    {
      "kind": "article",
      "name": "Striver's A2Z DSA sheet",
      "url": "https://takeuforward.org/strivers-a2z-dsa-course/strivers-a2z-dsa-course-sheet-2/",
      "note": "An ordered list of problems per topic, easy ones first."
    }
  ]
}