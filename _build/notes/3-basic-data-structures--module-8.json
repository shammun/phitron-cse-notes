{
  "module_id": "3-basic-data-structures--module-8",
  "title": "Singly linked list assignment problems",
  "overview": "This module is solving time: assignment problems that must be done with a singly linked list. Each one mixes the basic moves — build with `insert_at_tail`, walk with a `tmp` pointer, delete by relinking — into a small task: max minus min, search for an index, compare two lists, handle insert/delete queries and remove duplicates. Revising these shows how far the few core operations can take you.",
  "concepts": [
    {
      "term": "Read until a sentinel",
      "explain": "The input lists end with `-1`. A `while(true)` loop reads a value, `break`s on -1, and otherwise appends it to the list.",
      "snippet": "while(true){ cin >> v; if(v == -1) break; insert_at_tail(head, tail, v); }"
    },
    {
      "term": "One walk, one answer",
      "explain": "Max, min, size and search all use the same pattern: a `tmp` pointer from `head` to `NULL`, updating a running value (or a running index) at each node.",
      "snippet": "for(Node* t = head; t != NULL; t = t->next) { ... }"
    },
    {
      "term": "0-based index while walking",
      "explain": "Keep a counter that starts at 0 and goes up by one each time `tmp` moves; when the value matches, the counter is the index.",
      "snippet": null
    },
    {
      "term": "Walk two lists together",
      "explain": "To compare two lists, check the sizes first, then move `tmp1` and `tmp2` one step at a time and stop at the first different value.",
      "snippet": null
    },
    {
      "term": "Guard the index before deleting",
      "explain": "An index is valid only when `0 <= idx < size`. Index 0 is the head case, and when the removed node was the last one, `tail` must be moved back.",
      "snippet": "if(head == NULL || idx >= get_size(head)) return;"
    },
    {
      "term": "Remove duplicates with two pointers",
      "explain": "For each `outer` node, an `inner` pointer scans the rest of the list and unlinks every later node with the same value. The first occurrence of each value stays, so the order is kept.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "assignment2_problem_1_difference_max_min.cpp",
      "title": "Difference between max and min",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Reads a list and prints the largest value minus the smallest value, found with two separate walks.",
      "steps": [
        "Build the list until `-1`.",
        "`find_max` walks the list with `max` starting at `INT_MIN`.",
        "`find_min` walks it again with `min` starting at `INT_MAX`.",
        "Print `max - min`."
      ],
      "complexity": "Time O(n) · Space O(n) for the list",
      "input": "12 3 45 7 20 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "`42` = 45 (max) − 3 (min).",
      "watch_out": null,
      "check": {
        "q": "Could max and min be found in one walk?",
        "a": "Yes — update both running values inside the same loop."
      }
    },
    {
      "file": "assignment2_problem_1_difference_max_min_p.cpp",
      "title": "Max minus min (practice copy)",
      "kind": "variant",
      "same_as": "assignment2_problem_1_difference_max_min.cpp",
      "problem": null,
      "what": "Identical re-typing of the max–min difference solution.",
      "steps": [
        "Same two walks for max and min, then subtract."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "12 3 45 7 20 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "`42`.",
      "watch_out": null,
      "check": {
        "q": "What does the program print for a one-value list `9 -1`?",
        "a": "0, because max and min are both 9."
      }
    },
    {
      "file": "assignment2_problem_2_search.cpp",
      "title": "Search: first index of X",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "For each test case, builds a list and prints the 0-based index of the first node equal to `X`, or -1 if it is missing.",
      "steps": [
        "Read `T`, the number of test cases, and loop `while(T--)`.",
        "Each case starts with a fresh `head`/`tail` and reads values until -1.",
        "Read `X`, then `find_index` walks the list with `index` starting at 0.",
        "Return `index` at the first match; return -1 after the loop if nothing matched.",
        "Print the result on its own line."
      ],
      "complexity": "Time O(n) per test · Space O(n)",
      "input": "3\n5 8 2 7 -1\n2\n1 2 3 -1\n9\n4 4 6 -1\n4\n",
      "input_note": "T, then for each test: the list ended by -1, then X on the next line.",
      "output_note": "`2` (2 is at index 2), `-1` (9 is missing), `0` (the first 4 is at index 0 — the search stops at the first match).",
      "watch_out": null,
      "check": {
        "q": "Why is `head` declared inside the `while(T--)` loop?",
        "a": "So every test case starts with an empty list instead of adding to the previous one."
      }
    },
    {
      "file": "assignment2_problem_2_search_p.cpp",
      "title": "Search for X (practice copy)",
      "kind": "variant",
      "same_as": "assignment2_problem_2_search.cpp",
      "problem": null,
      "what": "Same solution, but the line that prints the answer is missing.",
      "steps": [
        "Same building and `find_index` walk; the result is stored in `index` and never used."
      ],
      "complexity": "Time O(n) per test · Space O(n)",
      "input": "3\n5 8 2 7 -1\n2\n1 2 3 -1\n9\n4 4 6 -1\n4\n",
      "input_note": "Same format as the original.",
      "output_note": "Empty output — the program computes the answers but never prints them.",
      "watch_out": "Add `cout << index << endl;` after `int index = find_index(head, X);`.",
      "check": {
        "q": "What should the three answers be for this input?",
        "a": "2, -1 and 0."
      }
    },
    {
      "file": "assignment3_problem_3_same_same.cpp",
      "title": "Are two lists exactly the same?",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Reads two lists and prints YES only if they have the same length and the same value at every position.",
      "steps": [
        "Build list 1 and list 2, each ended by -1.",
        "`is_similar` first compares `get_size` of both; different sizes → NO.",
        "Then `temp1` and `temp2` move together for `size1` steps.",
        "At the first position where the values differ, print NO and return.",
        "If the loop finishes, print YES."
      ],
      "complexity": "Time O(n + m) · Space O(n + m)",
      "input": "10 20 30 -1\n10 20 30 -1\n",
      "input_note": "Two lists, each ended by -1.",
      "output_note": "`YES` — same size and same values in the same order.",
      "watch_out": null,
      "check": {
        "q": "Why check sizes before comparing values?",
        "a": "Otherwise one pointer could reach `NULL` while the other still has nodes, and reading `->val` from `NULL` crashes."
      }
    },
    {
      "file": "assignment3_problem_3_same_same_p.cpp",
      "title": "Same lists (practice copy)",
      "kind": "variant",
      "same_as": "assignment3_problem_3_same_same.cpp",
      "problem": null,
      "what": "Same idea, but the comparison loop runs `while(tmp1 != NULL)` instead of counting to `size1`.",
      "steps": [
        "Size check first, then walk both lists until `tmp1` reaches the end."
      ],
      "complexity": "Time O(n + m) · Space O(n + m)",
      "input": "10 20 30 -1\n10 25 30 -1\n",
      "input_note": "Two lists, each ended by -1.",
      "output_note": "`NO` — the sizes match but position 1 holds 20 vs 25.",
      "watch_out": null,
      "check": {
        "q": "Is `while(tmp1 != NULL)` safe here?",
        "a": "Yes, because the sizes are already known to be equal, so `tmp2` runs out at the same moment."
      }
    },
    {
      "file": "assignment3_problem_4_queries.cpp",
      "title": "Insert/delete queries",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Starts with an empty list and handles Q queries: `0 V` inserts V at the head, `1 V` inserts V at the tail, `2 V` deletes index V if it is valid. After each query the list is printed.",
      "steps": [
        "Read `Q`, then for each query read `X` and `V`.",
        "`X == 0` → `insert_at_head`; `X == 1` → `insert_at_tail` (this version walks to the end each time).",
        "`X == 2` → `delete_at_any_position`, which returns early if the list is empty or `idx >= size`.",
        "Index 0 deletes the head and sets `tail = NULL` if the list became empty.",
        "Otherwise walk to `idx-1`, unlink the next node, and move `tail` back if the last node was removed.",
        "Print the list after every query."
      ],
      "complexity": "Time O(n) per query · Space O(n)",
      "input": "6\n0 10\n1 20\n0 5\n2 1\n2 7\n2 0\n",
      "input_note": "Q, then Q lines of `X V`.",
      "output_note": "Lines: `10`, `10 20`, `5 10 20`, `5 20` (index 1 removed), `5 20` (index 7 is invalid, nothing happens), `20` (head removed).",
      "watch_out": "If the list becomes empty the program prints an empty line — check whether the judge wants that. `insert_at_head` does not update `tail` for later inserts, which is fine only because `insert_at_tail` here walks from the head.",
      "check": {
        "q": "For a list of size 3, which delete indexes are valid?",
        "a": "0, 1 and 2."
      }
    },
    {
      "file": "assignment3_problem_4_queries_p.cpp",
      "title": "Insert/delete queries (practice copy)",
      "kind": "variant",
      "same_as": "assignment3_problem_4_queries.cpp",
      "problem": null,
      "what": "Same solution, except the validity check is `idx > get_size(head)` instead of `>=`.",
      "steps": [
        "Same query handling as the original."
      ],
      "complexity": "Time O(n) per query · Space O(n)",
      "input": "6\n0 10\n1 20\n0 5\n2 1\n2 7\n2 0\n",
      "input_note": "Q, then Q lines of `X V`.",
      "output_note": "Same output as the original for this input: `10`, `10 20`, `5 10 20`, `5 20`, `5 20`, `20`.",
      "watch_out": "Off-by-one: deleting index == size passes the check. For list `10 20`, the query `2 2` walks to the last node, then `tmp->next->next` reads from `NULL` and the program crashes. Use `idx >= get_size(head)`.",
      "check": {
        "q": "Why is `idx == size` invalid?",
        "a": "Indexes go from 0 to size−1, so there is no node at index `size`."
      }
    },
    {
      "file": "assignment3_problem5_remove_duplicate.cpp",
      "title": "Remove duplicate values",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Deletes every repeated value from the list, keeping the first occurrence of each, then prints the list.",
      "steps": [
        "`outer` walks the list one node at a time.",
        "For each `outer`, `inner` starts at `outer` and looks at `inner->next`.",
        "If `inner->next->val` equals `outer->val`, unlink and `delete` that node — and do NOT move `inner`, because the new `inner->next` still needs checking.",
        "Otherwise move `inner` forward.",
        "When `inner` reaches the end, move `outer` forward."
      ],
      "complexity": "Time O(n²) · Space O(1) extra",
      "input": "2 5 2 3 5 5 1 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "`2 5 3 1` — the later 2 and both later 5s were removed; the order of first occurrences is kept.",
      "watch_out": "This `insert_at_tail` walks the whole list for every value, so reading input is O(n²) even though `tail` is available. Using `tail->next = newNode` makes it O(1).",
      "check": {
        "q": "Why does `inner` not move after a deletion?",
        "a": "Its new `next` is an unchecked node; moving on would skip it and could leave two equal values side by side."
      }
    },
    {
      "file": "assignment3_problem5_remove_duplicate_p.cpp",
      "title": "Remove duplicates (practice copy)",
      "kind": "variant",
      "same_as": "assignment3_problem5_remove_duplicate.cpp",
      "problem": null,
      "what": "Same algorithm with the `if` written the other way round (test for equal first, then the else moves `inner`).",
      "steps": [
        "Identical two-pointer duplicate removal."
      ],
      "complexity": "Time O(n²) · Space O(1) extra",
      "input": "2 5 2 3 5 5 1 -1\n",
      "input_note": "Values ended by -1.",
      "output_note": "`2 5 3 1`.",
      "watch_out": null,
      "check": {
        "q": "If the list were sorted, how could duplicates be removed faster?",
        "a": "Equal values sit next to each other, so a single walk comparing each node with its `next` is enough — O(n)."
      }
    }
  ],
  "practice_links": [
    {
      "name": "Remove Duplicates from Sorted List",
      "url": "https://leetcode.com/problems/remove-duplicates-from-sorted-list/",
      "site": "LeetCode"
    },
    {
      "name": "Design Linked List",
      "url": "https://leetcode.com/problems/design-linked-list/",
      "site": "LeetCode"
    },
    {
      "name": "Intersection of Two Linked Lists",
      "url": "https://leetcode.com/problems/intersection-of-two-linked-lists/",
      "site": "LeetCode"
    },
    {
      "name": "Remove Linked List Elements",
      "url": "https://leetcode.com/problems/remove-linked-list-elements/",
      "site": "LeetCode"
    }
  ],
  "recap": [
    "Most list problems are one walk with a running value: size, max, min, index.",
    "Reset `head`/`tail` for every test case.",
    "Compare two lists: sizes first, then walk both together.",
    "A valid index satisfies `0 <= idx < size`; watch `>` vs `>=`.",
    "When a delete removes the last node, move `tail` too."
  ],
  "check_yourself": [
    {
      "q": "Why do the query solutions check `head == NULL` before deleting?",
      "a": "An empty list has no node to remove, and walking from a `NULL` head would crash."
    },
    {
      "q": "In duplicate removal, which occurrence of each value is kept?",
      "a": "The first one; only later copies are unlinked."
    },
    {
      "q": "What does `find_index` return for a value that appears twice?",
      "a": "The index of its first appearance, because it returns as soon as it finds a match."
    }
  ],
  "intro": [
    {
      "heading": null,
      "body": [
        "Nothing new is taught today. This is the assignment day for singly linked lists, and every problem on it is built from four moves you already have.",
        "**Build.** Read values until `-1` and append each one with `insert_at_tail(head, tail, v)`. **Walk.** Put a `tmp` on `head` and step `tmp = tmp->next` until `NULL`, updating one running value as you go. **Count while you walk.** Keep an index that starts at 0 and goes up one per step. **Delete.** Stand one node before the victim, bend the arrow past it, free it.",
        "If those four are in your fingers, the five problems here are just different things to put inside the walk."
      ],
      "snippet": "for(Node* t = head; t != NULL; t = t->next){\n    // one running value lives out here\n}",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "**One worked example, because the rest are variations of it.** The first problem asks for the largest value minus the smallest. Take the list `12 3 45 7 20`. Start `max` at a value smaller than anything real and walk once, keeping the best so far:",
        "The answer is 45 − 3 = 42. The page does this with two separate walks, one for `max` and one for `min`, but both running values could sit in the same loop — that is worth trying yourself.",
        "Now change only what you keep. Keep a counter instead of a maximum and stop at the first node equal to `X`: that is the search problem, and the counter is the index. Keep nothing and just count the steps: that is `get_size`. Same walk, different passenger."
      ],
      "snippet": null,
      "trace": "node seen   12   3   45   7   20\nmax so far  12  12   45  45   45\nmin so far  12   3    3   3    3"
    },
    {
      "heading": null,
      "body": [
        "**Two lists at once.** To ask whether two lists are identical, compare the sizes first, then move `tmp1` and `tmp2` one step together and stop at the first place they differ. The size check is not politeness: without it one pointer reaches `NULL` while the other still has nodes, and reading `->val` from `NULL` crashes.",
        "**Two pointers on one list.** Removing duplicates uses an `outer` node and an `inner` pointer that scans everything after it, unlinking each later node with the same value. On `2 5 2 3 5 5 1`:",
        "The first copy of each value stays, so the original order survives. One trap: after you unlink a node, do **not** move `inner` forward. Its new `next` is a node you have not looked at yet, and skipping it can leave two equal values sitting side by side."
      ],
      "snippet": null,
      "trace": "start        2 5 2 3 5 5 1\nouter = 2 -> 2 5 3 5 5 1   (later 2 gone)\nouter = 5 -> 2 5 3 1       (both later 5s gone)\nouter = 3 -> 2 5 3 1       (nothing to do)\nouter = 1 -> 2 5 3 1"
    },
    {
      "heading": null,
      "body": [
        "**What to watch for, since this is where marks are lost.** Declare `head` and `tail` inside the test-case loop, or case two quietly adds onto case one's list. A delete index is valid only when `0 <= idx < size`: writing `idx > size` instead of `idx >= size` lets `idx == size` through, and the walk then reads past the end of the list. When a delete removes the last node, `tail` has to move back too, otherwise the next `insert_at_tail` writes onto a freed box. And a pointer only moves with `j = j->next`; a bare `j->next` reads the address and throws it away, so the loop spins forever.",
        "The programs below are the five problems, each followed by a re-typed copy with one thing changed. The note under each copy says what broke and how the output gives it away."
      ],
      "snippet": null,
      "trace": null
    }
  ],
  "resources": [
    {
      "kind": "video",
      "name": "Striver — linked list playlist",
      "url": "https://www.youtube.com/playlist?list=PLgUwDviBIf0rAuz8tVcM0AymmhTRsfaLU",
      "note": "From the basics to the interview problems on lists."
    },
    {
      "kind": "visual",
      "name": "VisuAlgo — linked list",
      "url": "https://visualgo.net/en/list",
      "note": "Press Insert or Remove and watch the arrows re-point, one step at a time."
    },
    {
      "kind": "article",
      "name": "Striver's A2Z DSA sheet",
      "url": "https://takeuforward.org/strivers-a2z-dsa-course/strivers-a2z-dsa-course-sheet-2/",
      "note": "An ordered list of problems per topic, easy ones first."
    }
  ]
}