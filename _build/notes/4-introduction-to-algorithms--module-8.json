{
  "module_id": "4-introduction-to-algorithms--module-8",
  "title": "Mid-term: grid search problems",
  "overview": "The mid-term exam solutions. All four are grid problems: reachability with DFS, the smallest component area, knight moves with BFS on a chessboard, and printing the BFS path through a maze with a parent array. Together they are a good revision of Modules 2-6.",
  "concepts": [
    {
      "term": "Early-exit DFS",
      "explain": "Return true from DFS as soon as the target is found, and pass it up through every caller.",
      "snippet": null
    },
    {
      "term": "Min over components",
      "explain": "Start `min_area = INT_MAX`, update after each DFS, and use a flag to print -1 when no component exists.",
      "snippet": null
    },
    {
      "term": "Custom move sets",
      "explain": "A knight has 8 moves; put them in the `direction` list and BFS works unchanged.",
      "snippet": "{{1,2},{1,-2},{2,1},{2,-1},{-1,2},{-1,-2},{-2,1},{-2,-1}}"
    },
    {
      "term": "dist as visited",
      "explain": "Filling `dist` with -1 lets `dist[i][j] == -1` mean \"not visited\", so no separate `vis` is needed.",
      "snippet": null
    },
    {
      "term": "Path on a grid",
      "explain": "Store `path[child] = parent cell` during BFS, then walk back from the destination and mark cells.",
      "snippet": null
    },
    {
      "term": "Direction order matters",
      "explain": "When several shortest paths exist, the order of `direction` decides which one BFS records.",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "mid_term_1_same_component.cpp",
      "title": "Same component?",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Checks with DFS whether cell D can be reached from cell S through `.` cells.",
      "steps": [
        "Read the grid and the two cells.",
        "If either cell is `-`, print NO; if they are the same cell, print YES.",
        "`dfs(si, sj, di, dj)` returns true when it steps on D.",
        "A true result from any child is returned immediately."
      ],
      "complexity": "Time O(N·M) · Space O(N·M)",
      "input": "5 4\n....\n---.\n..-.\n--..\n....\n0 1\n3 2\n",
      "input_note": "`N M`, the grid, then `Si Sj`, then `Di Dj` (sample 2 of the statement).",
      "output_note": "`YES`: from (0,1) you go right along the top, down the right column and left to (3,2).",
      "watch_out": "On a 1000x1000 open grid the recursion can go 10^6 deep; BFS avoids that risk.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why check `grid[si][sj] == '-'` before searching?",
        "a": "A blocked start (or end) cell can never be in a walkable component."
      }
    },
    {
      "file": "mid_term_2_area_of_component.cpp",
      "title": "Minimum area of a component",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "DFS measures each `.` component; the smallest area is printed, or -1 if there are none.",
      "steps": [
        "`area = 0` before each DFS; `dfs` increments it.",
        "`min_area = min(min_area, area)` and `flag = true`.",
        "Print `min_area` if `flag`, else -1."
      ],
      "complexity": "Time O(N·M) · Space O(N·M)",
      "input": "6 5\n..-..\n..-..\n-----\n.-...\n.----\n.....\n",
      "input_note": "The 6x5 sample grid.",
      "output_note": "`3`: the pieces have 4, 4, 7 and 3 cells; the smallest is the three dots on the right of row 3.",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why is `flag` needed when `min_area` already exists?",
        "a": "Without components `min_area` stays `INT_MAX`, which must be turned into -1."
      }
    },
    {
      "file": "mid_term_3_knight_moves.cpp",
      "title": "Knight moves",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "BFS from the knight's cell with the 8 knight jumps; the level of the queen's cell is the answer.",
      "steps": [
        "For each test case read the board size and both cells.",
        "`bfs` fills `dist` with -1 and sets the knight's cell to 0.",
        "Pop a cell; if it is the queen's cell, return its `dist`.",
        "Push each valid unvisited jump with `dist + 1`; return -1 if the queue empties."
      ],
      "complexity": "Time O(T·N·M) · Space O(N·M)",
      "input": "3\n8 8\n0 0\n7 7\n2 2\n0 0\n0 1\n3 3\n0 0\n2 1\n",
      "input_note": "`T`, then for each case: `N M`, knight cell, queen cell. Cases: 8x8 corner to corner, a 2x2 board, and a 3x3 board.",
      "output_note": "`6` (8x8 corner to corner), `-1` (no knight move fits on a 2x2 board), `1` (one jump from (0,0) to (2,1)).",
      "watch_out": null,
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why is BFS, not DFS, right for the minimum number of knight moves?",
        "a": "Every move costs the same, so BFS levels are the fewest moves."
      }
    },
    {
      "file": "mid_term_4_maze.cpp",
      "title": "Maze path marking",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "BFS from R to D with a `path` parent grid, then walks back from D and puts `X` on every `.` cell of the route.",
      "steps": [
        "Find R and D while reading; `direction` is right, left, up, down as the statement demands.",
        "`bfs` records `path[child] = {par_i, par_j}` for each new cell.",
        "If D was reached, step back from D using `path` until the parent is R.",
        "Each `.` cell on the way becomes `X`; then the maze is printed."
      ],
      "complexity": "Time O(N·M) · Space O(N·M)",
      "input": "5 6\n...D.#\n.##..#\n....#.\n.R#...\n.#.##.\n",
      "input_note": "`N M`, then the maze (sample 0 of the statement).",
      "output_note": "The route is marked with `X`: from R go up into row 2, right along row 2 to column 3, then up through row 1 to D. R and D keep their letters.",
      "watch_out": "The `if(s_i == -1 || d_i == -1)` check never fires, because the positions start at 0, not -1. Start them at -1 if you want that guard to work.",
      "prelude": null,
      "driver": null,
      "check": {
        "q": "Why does the walk-back stop when the parent equals R instead of when the cell equals R?",
        "a": "So the loop never tries to mark R itself, and it ends one step early."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "Reachability: DFS with early return, or BFS.",
    "Min/max over components: reset a counter before each search.",
    "Change the move list for knights or diagonals.",
    "Path printing on grids: parent grid + walk back."
  ],
  "check_yourself": [
    {
      "q": "How many moves does a knight have, and how many can go off the board from a corner?",
      "a": "8 moves; from a corner only 2 stay on the board, so 6 go off."
    },
    {
      "q": "How would you print the maze path length instead of marking it?",
      "a": "Keep a `dist` grid in BFS and print `dist` at D."
    }
  ]
}