{
  "module_id": "4-introduction-to-algorithms--mock-contest",
  "title": "Mock contest: search, sets, number reasoning, greedy, partition DP",
  "overview": "A practice contest before the final. The first three problems need simple tools: a linear search, a `set` for unique sorted letters, and a little number reasoning. The last two files hold only the problem statements, so the notes explain how to attack them: a sort-then-greedy idea and a subset DP with a size limit.",
  "concepts": [
    {"term": "Linear search", "explain": "Scan from the left and return the first index where the value matches; return -1 if you reach the end.", "snippet": null},
    {"term": "std::set", "explain": "Keeps unique elements in sorted order automatically, so inserting every character and then printing the set gives unique letters alphabetically.", "snippet": "set<char> s(str.begin(), str.end());"},
    {"term": "Reachable sums with 3 and 4", "explain": "3, 4, 6, 7, 8 are reachable, and since 6, 7, 8 are three consecutive values, adding 3 again covers every number from 6 upward. Only 1, 2 and 5 are impossible.", "snippet": null},
    {"term": "Sort, then greedy with a falling cap", "explain": "To take distinct counts from bags, sort sizes from large to small and take `min(size, previous - 1)`, never below 0.", "snippet": null},
    {"term": "Balanced partition with a size rule", "explain": "Choose exactly n/2 items whose sum is as close as possible to half the total. A DP over (items used, sum) records which sums are reachable.", "snippet": null}
  ],
  "files": [
    {
      "file": "1_The Lost Book.cpp",
      "title": "The Lost Book (linear search)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Returns the 0-based index of the target code in the array, or -1 if it is missing.",
      "steps": [
        "Read `n`, the array, and `target`.",
        "`lostBook` loops `i` from 0 to n-1 and returns `i` at the first match.",
        "If the loop ends, return -1.",
        "Print the result."
      ],
      "complexity": "Time O(n) · Space O(n)",
      "input": "5\n10 20 5 6 3\n3\n",
      "input_note": "n, the book codes, then the target. This is the contest's first sample.",
      "output_note": "Prints `4`, because code 3 is at index 4 (counting from 0).",
      "watch_out": "The array is not sorted, so binary search would be wrong here.",
      "check": {"q": "What would the program print for target 99 with the same array?", "a": "-1"}
    },
    {
      "file": "2_Letters_in_Order.cpp",
      "title": "Letters in Order (set of characters)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Puts every character into a `set<char>`, which removes duplicates and sorts them, then prints the set.",
      "steps": [
        "Read the string `S`.",
        "Insert each character into `unique_letters`.",
        "Loop over the set and print each character (already in alphabetical order)."
      ],
      "complexity": "Time O(|S| log 26) · Space O(26)",
      "input": "banana\n",
      "input_note": "One lowercase string. This is the contest's second sample.",
      "output_note": "Prints `abn`: the letters b, a, n without repeats, in alphabetical order.",
      "watch_out": null,
      "check": {"q": "How could you do this without a set?", "a": "Use a `bool seen[26]` array, mark each letter, then print the marked letters from a to z."}
    },
    {
      "file": "3_Guess the No.cpp",
      "title": "Guess the No (sums of 3 and 4)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Decides whether T can be written as a sum of 3s and 4s. It lists the small reachable values and says YES for everything from 12 up.",
      "steps": [
        "`T < 3`: NO.",
        "T in {3, 4, 6, 7, 8, 9, 10, 11} or `T >= 12`: YES.",
        "Anything else (only 5 is left): NO."
      ],
      "complexity": "Time O(1) · Space O(1)",
      "input": "5\n",
      "input_note": "A single T. 5 is the interesting case: 3 + something never makes 5.",
      "output_note": "Prints `NO`. With T = 10 it prints YES (3 + 3 + 4).",
      "watch_out": "The long condition works, but the rule is simply: NO for 1, 2 and 5, YES for everything else.",
      "check": {"q": "Is 13 reachable? Give a split.", "a": "Yes: 3 + 3 + 3 + 4."}
    },
    {
      "file": "4_Here Comes the Genie.cpp",
      "title": "Here Comes the Genie (statement only)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Take balls from bags so that the counts taken from different colours are all different (zeros may repeat), maximising the total. The file contains only the problem statement.",
      "steps": [
        "Sort the bag sizes from largest to smallest.",
        "Take all balls from the largest bag: `cap = a[0]`.",
        "For every next bag take `cap = min(a[i], cap - 1)`; once `cap` reaches 0, take 0 from the rest.",
        "Add up the amounts in a `long long`.",
        "Why it works: the biggest bags should get the biggest distinct counts, and each count should be as large as allowed."
      ],
      "complexity": "Time O(n log n) · Space O(1) extra",
      "input": null,
      "input_note": "The file has no code, so nothing runs.",
      "output_note": "No output. With the plan above, the samples work out: [1, 4, 5] -> 5 + 4 + 1 = 10, and [1, 1, 2, 1] -> 2 + 1 + 0 + 0 = 3.",
      "watch_out": "Only the statement was saved; there is no solution in this file. Use `long long` for the total (up to 2x10^5 values of 10^9).",
      "check": {"q": "Bags [5, 5, 5]: what is the maximum?", "a": "12 = 5 + 4 + 3."}
    },
    {
      "file": "5_Mina_and_Moni.cpp",
      "title": "Mina and Moni (statement only)",
      "kind": "exam",
      "same_as": null,
      "problem": null,
      "what": "Split n coins into two groups of sizes n/2 and n - n/2 so the difference of their sums is as small as possible. The file contains only the problem statement.",
      "steps": [
        "Let `total` be the sum and `k = n / 2` the size of the smaller group.",
        "Build `can[c][s]`: can exactly `c` coins sum to `s`? Start with `can[0][0] = true`.",
        "For each coin, go through `c` from high to low and mark `can[c+1][s + coin]` from `can[c][s]` (0-1 knapsack style).",
        "For every `s` with `can[k][s]`, the difference is `abs(total - 2*s)`; print the smallest.",
        "Use a bitset per count if the sums are large."
      ],
      "complexity": "Time O(n·k·total) (faster with bitsets) · Space O(k·total)",
      "input": null,
      "input_note": "The file has no code, so nothing runs.",
      "output_note": "No output. For coins 1 2 3 10 the best split is {1, 10} vs {2, 3}, difference 11 - 5 = 6, matching the sample.",
      "watch_out": "Only the statement was saved; there is no solution in this file. Plain subset sum is not enough: the group sizes are fixed, so the DP must also track how many coins are used.",
      "check": {"q": "Why must the DP track the number of coins, not only the sum?", "a": "Each sister must get exactly n/2 (or n/2 + 1) coins; a subset with the right sum but the wrong size is not allowed."}
    }
  ],
  "practice_links": [
    {"name": "Partition Array Into Two Arrays to Minimize Sum Difference", "url": "https://leetcode.com/problems/partition-array-into-two-arrays-to-minimize-sum-difference/", "site": "LeetCode"},
    {"name": "Minimum Deletions to Make Character Frequencies Unique", "url": "https://leetcode.com/problems/minimum-deletions-to-make-character-frequencies-unique/", "site": "LeetCode"}
  ],
  "recap": [
    "Unsorted data: linear search; do not binary search.",
    "`set` gives unique + sorted in one step.",
    "For 'sums of a and b' problems, find the small impossible values; beyond them everything works.",
    "Sort descending and use a falling cap to make counts distinct.",
    "Fixed group sizes turn subset sum into a DP over (count, sum)."
  ],
  "check_yourself": [
    {"q": "Which values cannot be made from 5s and 3s?", "a": "1, 2, 4 and 7. From 8 on everything works (8, 9, 10 are reachable and adding 3 covers the rest)."},
    {"q": "What is the running time of inserting m characters into a `set<char>`?", "a": "O(m log k), where k (at most 26) is the number of distinct letters, so effectively O(m)."}
  ]
}
