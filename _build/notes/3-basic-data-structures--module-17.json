{
  "module_id": "3-basic-data-structures--module-17",
  "title": "Binary trees & depth-first traversals",
  "overview": "This module starts trees. You build a small binary tree by hand with `Node` objects that have `left` and `right` pointers, then visit every node with the three depth-first orders: pre-order, in-order and post-order. The three orders are the same recursion with the print moved to a different place.",
  "intro": [
    {
      "heading": null,
      "body": [
        "Trees start here, and you already have almost everything you need. A linked list node held a value and one pointer, `next`, so the nodes formed a single line and a walk had only one way to go. Now change one thing: give the node **two** pointers instead of one, and call them `left` and `right`.",
        "That is the whole definition of a binary tree node. A missing child is `NULL`, exactly as the end of a list was `NULL`."
      ],
      "snippet": "class Node {\npublic:\n    int val;\n    Node* left;    // where a list had one next,\n    Node* right;   // a tree node has two\n    Node(int val){\n        this->val = val;\n        this->left = NULL;\n        this->right = NULL;\n    }\n};",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Draw it and the consequence shows up at once. With one pointer the nodes lie in a line. With two, every node can send you two ways, so the picture spreads out downwards. Here is the small tree the programs on this page build by hand, with six nodes."
      ],
      "snippet": null,
      "trace": "a linked list:   10 -> 20 -> 40 -> NULL\n\nthe same 6 values as a tree:\n\n              10\n             /  \\\n           20    30\n          /     /  \\\n        40    50    60"
    },
    {
      "heading": null,
      "body": [
        "Five words, all readable straight off that picture. The **root** is `10`: the node at the top, the one nothing points to, and the one pointer you hold on to, just as `head` was your only handle on a list. A **child** hangs directly under another node, so `20` and `30` are the children of `10`, and `10` is their **parent**. A **leaf** has no children at all, both pointers `NULL`: here `40`, `50` and `60`. A **subtree** is any node together with everything under it, so `30`, `50`, `60` form the right subtree of the root, and a subtree is itself a perfectly good tree.",
        "**Height** is how deep the picture goes. The longest path down is `10`, `20`, `40`: three nodes on three levels, so height 3. Some books count the two steps between them instead and say 2, so check which a problem means.",
        "The new thing compared with lists is not the node, it is the walking. A list had one route, so a `while` loop did the job. Here the route splits in two at every node, and a loop cannot be in two places at once. Recursion can: a tree is a root plus a left subtree plus a right subtree, and each subtree gets the same function called on it. The base case is the empty tree."
      ],
      "snippet": "if(root == NULL) return;   // nothing here, go back",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "So a full traversal is three lines: handle this node, go left, go right. And here is the point of the module. Those three lines can be written in different orders, and only the **position of the visit** changes. Move the `cout` and you get a different traversal with the same two recursive calls."
      ],
      "snippet": "void preorder(Node* root){        // Root, Left, Right\n    if(root == NULL) return;\n    cout << root->val << \" \";   // visit before both\n    preorder(root->left);\n    preorder(root->right);\n}\n// cout between the two calls -> in-order\n// cout after both calls       -> post-order",
      "trace": null
    },
    {
      "heading": null,
      "body": [
        "Traced on the same little tree, the three orders come out like this. Follow one of them with a finger on the drawing above and you will see the walk is identical every time; only the moment a value is printed moves."
      ],
      "snippet": null,
      "trace": "pre-order   Root, Left, Right\n   10  20  40  30  50  60     root first\n\nin-order    Left, Root, Right\n   40  20  10  50  30  60     root in the middle\n\npost-order  Left, Right, Root\n   40  20  50  60  30  10     root last"
    },
    {
      "heading": null,
      "body": [
        "Each order suits a different job. Pre-order sees a parent before its children, which is what you want when copying a tree. Post-order finishes both children before the parent, which is what deleting a tree needs: free the children first, because once the parent is gone you have lost the pointers to them. In-order becomes the interesting one in a later module, where a tree is arranged so that in-order hands the values back already sorted.",
        "All three visit every node once, so each is O(n), and the memory is the depth of the recursion, O(h) for height h. One caution while you experiment: `root->left->right->val` crashes on this tree, because `20` has no right child. Know a child exists before you follow it.",
        "The four programs below take one step each: build the tree by hand, then add pre-order, then in-order, then post-order, each keeping the earlier functions so you see all three outputs together."
      ],
      "snippet": null,
      "trace": null
    }
  ],
  "concepts": [
    {
      "term": "Binary tree node",
      "explain": "Each node stores a value and two pointers, `left` and `right`. A missing child is `NULL`. The top node is the **root**; a node with no children is a **leaf**.",
      "snippet": "class Node { public: int val; Node* left; Node* right; };"
    },
    {
      "term": "Linking nodes by hand",
      "explain": "Create nodes with `new Node(x)` and connect them by assigning `root->left = a;` and so on. You reach deeper nodes by chaining arrows, like `root->right->left`.",
      "snippet": null
    },
    {
      "term": "Recursion on a tree",
      "explain": "A tree is a root plus a left subtree plus a right subtree. So a function can handle the root and call itself on the two subtrees. The base case is `root == NULL`: nothing to do.",
      "snippet": "if(root == NULL) return;"
    },
    {
      "term": "Pre-order (Root, Left, Right)",
      "explain": "Print the node first, then visit left, then right. The root always comes first in the output.",
      "snippet": "cout << root->val; preorder(root->left); preorder(root->right);"
    },
    {
      "term": "In-order (Left, Root, Right)",
      "explain": "Visit the whole left subtree, then print the node, then the right subtree. The root ends up in the middle.",
      "snippet": null
    },
    {
      "term": "Post-order (Left, Right, Root)",
      "explain": "Visit both subtrees first and print the node last. The root always comes last, which is handy when children must be handled before parents (for example, deleting a tree).",
      "snippet": null
    }
  ],
  "files": [
    {
      "file": "binary_tree.cpp",
      "title": "Build a binary tree by hand",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Creates six nodes and links them into a tree: 10 is the root, 20 and 30 are its children, 40 hangs under 20, and 50, 60 hang under 30. Then it prints each value by walking the pointers.",
      "steps": [
        "`Node(int val)` sets the value and makes `left` and `right` `NULL`.",
        "`main` creates `root` (10) and nodes `a`..`e` (20..60).",
        "Links: `root->left = a`, `root->right = b`, `a->left = c`, `b->left = d`, `b->right = e`.",
        "Each `cout` reaches a node from the root, e.g. `root->right->left->val` is 50."
      ],
      "complexity": null,
      "input": null,
      "input_note": null,
      "output_note": "Six lines, 10 to 60, confirming each pointer path reaches the node you expected.",
      "watch_out": "Reading `root->left->right->val` here would crash: `a->right` is `NULL`. Always know a child exists before following it.",
      "check": {
        "q": "Which nodes are leaves in this tree?",
        "a": "40, 50 and 60. They have no children."
      }
    },
    {
      "file": "pre_order_traversal.cpp",
      "title": "Pre-order traversal",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Same hand-built tree, plus a recursive `preorder` that prints Root, then Left subtree, then Right subtree.",
      "steps": [
        "`preorder(root)` returns at once if `root` is `NULL`.",
        "Otherwise it prints `root->val` first.",
        "Then it calls `preorder(root->left)` and finishes that whole side.",
        "Finally it calls `preorder(root->right)`.",
        "For our tree: 10, then left side (20, 40), then right side (30, 50, 60)."
      ],
      "complexity": "Time O(n) · Space O(h) for the call stack (h = height)",
      "input": null,
      "input_note": null,
      "output_note": "After the six value lines: `Pre-order traversal: 10 20 40 30 50 60`. The root 10 is first.",
      "watch_out": null,
      "check": {
        "q": "In pre-order, which value is always printed first?",
        "a": "The root's value."
      }
    },
    {
      "file": "in_order_traversal.cpp",
      "title": "In-order traversal",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Adds `inorder`, which prints Left subtree, then Root, then Right subtree, and prints both pre-order and in-order.",
      "steps": [
        "`inorder(root)` stops on `NULL`.",
        "It first calls `inorder(root->left)` so the entire left side prints.",
        "Then it prints `root->val`.",
        "Then `inorder(root->right)`.",
        "For our tree: 40 20 (left side), 10 (root), 50 30 60 (right side)."
      ],
      "complexity": "Time O(n) · Space O(h)",
      "input": null,
      "input_note": null,
      "output_note": "The last line reads `Pre-order traversal: 10 20 40 30 50 60 In-order traversal: 40 20 10 50 30 60` — both on one line.",
      "watch_out": "There is no `endl` after `preorder(root)`, so the two traversals run together on one line. Add `cout << endl;` after each call to separate them.",
      "check": {
        "q": "Where does the root 10 appear in the in-order output, and why?",
        "a": "In the middle (after 40 20), because the whole left subtree is printed before the root."
      }
    },
    {
      "file": "post_order_traversal.cpp",
      "title": "Post-order traversal",
      "kind": "lesson",
      "same_as": null,
      "problem": null,
      "what": "Adds `postorder`, which prints Left subtree, then Right subtree, then Root, and prints all three orders.",
      "steps": [
        "`postorder(root)` stops on `NULL`.",
        "It calls `postorder(root->left)`, then `postorder(root->right)`.",
        "Only after both children are done does it print `root->val`.",
        "For our tree: 40 20 (left side), 50 60 30 (right side), then 10."
      ],
      "complexity": "Time O(n) · Space O(h)",
      "input": null,
      "input_note": null,
      "output_note": "The last line holds all three: pre-order `10 20 40 30 50 60`, in-order `40 20 10 50 30 60`, post-order `40 20 50 60 30 10`.",
      "watch_out": "Same missing `endl` as before, so all three orders share a line. Also, the header comment says 'In-order' but this file is about post-order.",
      "check": {
        "q": "In post-order, which value is always printed last?",
        "a": "The root's value (10 here)."
      }
    }
  ],
  "practice_links": [],
  "recap": [
    "A binary tree node has a value plus `left` and `right` pointers; `NULL` means no child.",
    "Tree recursion: handle `NULL`, then the node, then the two subtrees.",
    "Pre-order = Root L R, In-order = L Root R, Post-order = L R Root.",
    "Only the position of the print line changes between the three traversals.",
    "Each traversal visits every node once: O(n) time."
  ],
  "check_yourself": [
    {
      "q": "A tree has root 1, left child 2, right child 3, and 2 has a left child 4. Give all three traversals.",
      "a": "Pre: 1 2 4 3. In: 4 2 1 3. Post: 4 2 3 1."
    },
    {
      "q": "What stops the traversal recursion?",
      "a": "Reaching a `NULL` child: the function returns without doing anything."
    },
    {
      "q": "Why is the extra memory O(h) and not O(1)?",
      "a": "Each recursive call waits on the call stack, and at most one chain from the root to a leaf (height h) is waiting at a time."
    }
  ],
  "resources": [
    {
      "kind": "visual",
      "name": "VisuAlgo — binary search tree",
      "url": "https://visualgo.net/en/bst",
      "note": "Insert values and watch the tree grow; the traversals are animated too."
    },
    {
      "kind": "visual",
      "name": "USFCA — binary search tree",
      "url": "https://www.cs.usfca.edu/~galles/visualization/BST.html",
      "note": "Insert, delete and search animated on the tree."
    },
    {
      "kind": "article",
      "name": "GeeksforGeeks — binary tree",
      "url": "https://www.geeksforgeeks.org/binary-tree-data-structure/",
      "note": "Vocabulary and every traversal in one place."
    },
    {
      "kind": "video",
      "name": "Striver — binary trees and BSTs",
      "url": "https://www.youtube.com/playlist?list=PLgUwDviBIf0q8Hkd7bK2Bpryj2xVJk8Vk",
      "note": "Traversals, views and the classic tree problems, in order."
    }
  ]
}